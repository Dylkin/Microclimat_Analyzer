# Развертывание Microclimat Analyzer на Ubuntu 24

Полный скрипт автоматического развертывания продакшена на Ubuntu 24 с нуля.

## 📋 Требования

- Ubuntu 24.04 LTS (или новее)
- Права root (sudo)
- Доступ к интернету
- Минимум 2GB RAM
- 10GB свободного места на диске

## 🚀 Быстрый старт

1. **Скачайте скрипт на сервер:**

```bash
wget https://raw.githubusercontent.com/Dylkin/Microclimat_Analyzer/main/deploy-production-ubuntu24.sh
```

Или скопируйте файл `deploy-production-ubuntu24.sh` на сервер.

2. **Сделайте скрипт исполняемым:**

```bash
chmod +x deploy-production-ubuntu24.sh
```

3. **Запустите скрипт:**

```bash
sudo ./deploy-production-ubuntu24.sh
```

4. **Следуйте инструкциям на экране:**

Скрипт запросит у вас:
- Имя пользователя для проекта (по умолчанию: `stas`)
- Директорию проекта (по умолчанию: `/home/stas/Microclimat_Analyzer`)
- URL репозитория GitHub
- Ветку для развертывания (по умолчанию: `main`)
- Доменное имя (обязательно)
- Параметры базы данных (имя БД, пользователь, пароль)
- Порт бэкенда (по умолчанию: `3001`)

## 📦 Что устанавливает скрипт

1. **Системные обновления** - обновляет систему до последних версий
2. **Node.js 20.x LTS** - устанавливает Node.js и npm
3. **PostgreSQL** - устанавливает и настраивает базу данных
4. **Nginx** - устанавливает веб-сервер
5. **PM2** - устанавливает менеджер процессов Node.js
6. **Git** - для клонирования репозитория
7. **Проект** - клонирует репозиторий из GitHub
8. **Зависимости** - устанавливает npm пакеты
9. **База данных** - создает БД и применяет миграции
10. **Сборка** - собирает фронтенд проект
11. **Конфигурация** - настраивает Nginx и PM2
12. **Запуск** - запускает все сервисы

## ⚙️ Конфигурация

### Файл .env

Скрипт автоматически создает файл `.env` в директории проекта со следующим содержимым:

```env
# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=microclimat
DB_USER=microclimat_user
DB_PASSWORD=your_password

# Сервер
PORT=3001
NODE_ENV=production

# CORS
CORS_ORIGIN=https://your-domain.com
```

### Nginx конфигурация

Скрипт создает конфигурацию Nginx в `/etc/nginx/sites-available/microclimat-analyzer`:
- Статические файлы из `dist/`
- Проксирование API на `http://localhost:3001`
- Gzip сжатие
- Кэширование статических ресурсов
- Проксирование загрузок из `/uploads`

### PM2 конфигурация

Используется файл `ecosystem.config.cjs` из репозитория. Скрипт автоматически обновляет пути в конфигурации.

## 🔍 Проверка развертывания

После завершения скрипта проверьте:

1. **Статус сервисов:**

```bash
# PM2
sudo -u stas pm2 status

# Nginx
systemctl status nginx

# PostgreSQL
systemctl status postgresql
```

2. **Доступность приложения:**

```bash
# Health check API
curl http://localhost:3001/health

# Через домен
curl http://your-domain.com/api/health
```

3. **Логи:**

```bash
# PM2 логи
sudo -u stas pm2 logs

# Nginx логи
tail -f /var/log/nginx/microclimat-error.log
tail -f /var/log/nginx/microclimat-access.log
```

## 🛠️ Полезные команды

### PM2

```bash
# Просмотр статуса
sudo -u stas pm2 status

# Просмотр логов
sudo -u stas pm2 logs

# Перезапуск
sudo -u stas pm2 restart all

# Остановка
sudo -u stas pm2 stop all

# Удаление
sudo -u stas pm2 delete all
```

### Nginx

```bash
# Перезагрузка конфигурации
systemctl reload nginx

# Проверка конфигурации
nginx -t

# Просмотр логов
tail -f /var/log/nginx/microclimat-error.log
```

### База данных

```bash
# Подключение к БД
sudo -u postgres psql -d microclimat

# Список таблиц
sudo -u postgres psql -d microclimat -c "\dt"

# Резервная копия
sudo -u postgres pg_dump microclimat > backup.sql
```

## 🔄 Обновление приложения

После развертывания для обновления приложения:

```bash
# Перейдите в директорию проекта
cd /home/stas/Microclimat_Analyzer

# Обновите код
git pull origin main

# Установите зависимости (если нужно)
npm install

# Примените миграции БД (если нужно)
npm run setup-db

# Пересоберите проект
npm run build

# Перезапустите PM2
pm2 restart all

# Перезагрузите Nginx
systemctl reload nginx
```

## 🔒 Безопасность

### Рекомендации:

1. **Firewall:** Настройте UFW для ограничения доступа:

```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp  # HTTPS (если используете SSL)
sudo ufw enable
```

2. **SSL/TLS:** Настройте SSL сертификат (Let's Encrypt):

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

3. **Пароли:** Используйте сильные пароли для базы данных

4. **Права доступа:** Файл `.env` должен иметь права `600` (только владелец)

## 🐛 Устранение неполадок

### Проблема: PM2 процесс не запускается

```bash
# Проверьте логи
sudo -u stas pm2 logs

# Проверьте файл .env
cat /home/stas/Microclimat_Analyzer/.env

# Проверьте права доступа
ls -la /home/stas/Microclimat_Analyzer
```

### Проблема: Nginx не проксирует API

```bash
# Проверьте конфигурацию
nginx -t

# Проверьте логи
tail -f /var/log/nginx/microclimat-error.log

# Проверьте, что бэкенд работает
curl http://localhost:3001/health
```

### Проблема: База данных недоступна

```bash
# Проверьте статус PostgreSQL
systemctl status postgresql

# Проверьте подключение
sudo -u postgres psql -d microclimat -c "SELECT 1;"

# Проверьте файл .env
cat /home/stas/Microclimat_Analyzer/.env
```

### Проблема: Фронтенд не загружается

```bash
# Проверьте, что dist/ существует
ls -la /home/stas/Microclimat_Analyzer/dist

# Проверьте права доступа
ls -la /home/stas/Microclimat_Analyzer/dist/index.html

# Проверьте логи Nginx
tail -f /var/log/nginx/microclimat-error.log
```

## 📝 Примечания

- Скрипт требует интерактивного ввода параметров
- Все пароли вводятся без отображения (безопасность)
- Скрипт можно запускать повторно (идемпотентен)
- При повторном запуске скрипт спросит, перезаписать ли существующие файлы

## 🔗 Дополнительные ресурсы

- [Документация PM2](https://pm2.keymetrics.io/docs/usage/quick-start/)
- [Документация Nginx](https://nginx.org/en/docs/)
- [Документация PostgreSQL](https://www.postgresql.org/docs/)
- [Документация Node.js](https://nodejs.org/en/docs/)

## 📧 Поддержка

При возникновении проблем проверьте логи и создайте issue в репозитории GitHub.

