# 🔄 Обновление продакшена

Инструкция по обновлению приложения Microclimat Analyzer на production-сервере.

## 📋 Быстрый старт

### Linux/macOS

```bash
# Сделать скрипт исполняемым (один раз)
chmod +x update-prod.sh

# Запустить обновление
./update-prod.sh
```

### Windows (PowerShell)

```powershell
# Запустить обновление
.\update-prod.ps1
```

## 📝 Что делает скрипт

Скрипт автоматически выполняет следующие действия:

1. ✅ **Создание резервной копии** (опционально)
   - Сохраняет текущее состояние проекта
   - Исключает `node_modules`, `dist`, `.git`

2. ✅ **Обновление кода из Git**
   - Получает последние изменения из репозитория
   - Проверяет наличие обновлений

3. ✅ **Установка зависимостей**
   - Обновляет npm-пакеты
   - Устанавливает новые зависимости

4. ✅ **Обновление базы данных** (опционально)
   - Применяет миграции
   - Обновляет структуру таблиц
   - Создает недостающие поля

5. ✅ **Сборка фронтенда**
   - Компилирует React-приложение
   - Создает production-сборку в `dist/`

6. ✅ **Перезапуск сервисов** (опционально)
   - PM2 (если используется)
   - Systemd сервисы
   - Nginx/IIS

7. ✅ **Проверка работоспособности**
   - Проверяет наличие файлов сборки
   - Проверяет статус сервисов

## ⚙️ Настройка

### Переменные окружения

Вы можете установить переменную `PROJECT_DIR` для указания пути к проекту:

**Linux/macOS:**
```bash
export PROJECT_DIR="/var/www/Microclimat_Analyzer"
./update-prod.sh
```

**Windows:**
```powershell
$env:PROJECT_DIR = "C:\var\www\Microclimat_Analyzer"
.\update-prod.ps1
```

### Автоматическое определение

Скрипт автоматически определяет путь к проекту, если:
- Запущен из директории проекта (где есть `package.json`)
- Проект находится в стандартной директории (`/var/www/Microclimat_Analyzer` для Linux, `C:\var\www\Microclimat_Analyzer` для Windows)

## 🔧 Ручное обновление

Если вы предпочитаете обновлять вручную:

### 1. Обновление кода

```bash
cd /path/to/project
git pull origin main  # или ваша ветка
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Обновление базы данных

```bash
npm run setup-db
```

### 4. Сборка фронтенда

```bash
npm run build
```

### 5. Перезапуск сервисов

**PM2:**
```bash
pm2 restart all
# или
pm2 restart microclimat-server
```

**Systemd:**
```bash
sudo systemctl restart microclimat-api
```

**Nginx:**
```bash
sudo systemctl restart nginx
# или
sudo service nginx restart
```

## 🚨 Решение проблем

### Ошибка: "Директория проекта не найдена"

**Решение:** Укажите путь к проекту явно:
```bash
PROJECT_DIR="/path/to/project" ./update-prod.sh
```

### Ошибка: "Git не установлен"

**Решение:** Установите Git:
```bash
# Ubuntu/Debian
sudo apt install git

# Windows
# Скачайте с https://git-scm.com/download/win
```

### Ошибка при обновлении базы данных

**Решение:** 
1. Проверьте файл `.env` и настройки подключения к БД
2. Убедитесь, что PostgreSQL запущен
3. Проверьте логи: `/tmp/db-update.log` (Linux) или `%TEMP%\db-update.log` (Windows)

### Ошибка при сборке фронтенда

**Решение:**
1. Проверьте версию Node.js: `node --version` (должна быть 18+)
2. Очистите кэш: `npm cache clean --force`
3. Удалите `node_modules` и переустановите: `rm -rf node_modules && npm install`

### Сервисы не перезапускаются

**Решение:** Перезапустите вручную:
```bash
# PM2
pm2 restart all

# Systemd
sudo systemctl restart microclimat-api

# Nginx
sudo systemctl restart nginx
```

### Ошибка 413 (Request Entity Too Large) при загрузке файлов

**Решение:** Увеличьте лимит размера загружаемых файлов в Nginx:

1. Откройте конфигурационный файл Nginx (обычно `/etc/nginx/nginx.conf` или `/etc/nginx/sites-available/default`)

2. Добавьте или измените директиву `client_max_body_size` в блоке `http` или `server`:

```nginx
http {
    # ... другие настройки ...
    client_max_body_size 100M;  # Увеличьте до нужного размера (например, 100MB)
    
    server {
        # ... настройки сервера ...
        client_max_body_size 100M;  # Можно также указать в блоке server
    }
}
```

3. Проверьте конфигурацию Nginx:
```bash
sudo nginx -t
```

4. Перезапустите Nginx:
```bash
sudo systemctl restart nginx
# или
sudo service nginx restart
```

**Примечание:** Лимит в Express уже увеличен до 100MB в `server/index.ts`. Убедитесь, что лимит в Nginx не меньше лимита в Express.

## 📚 Дополнительная информация

- Полная документация по развертыванию: [`DEPLOYMENT.md`](./DEPLOYMENT.md)
- Настройка базы данных: [`POSTGRESQL_SETUP.md`](./POSTGRESQL_SETUP.md)
- Быстрое развертывание: [`QUICK_DEPLOY.md`](./QUICK_DEPLOY.md)

## 🔐 Безопасность

⚠️ **Важно:**
- Всегда создавайте резервную копию перед обновлением
- Тестируйте обновления на тестовом сервере перед применением на продакшене
- Проверяйте логи после обновления
- Убедитесь, что у вас есть доступ для отката изменений

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервисов
2. Проверьте логи скрипта обновления
3. Убедитесь, что все зависимости установлены
4. Проверьте настройки в `.env`

