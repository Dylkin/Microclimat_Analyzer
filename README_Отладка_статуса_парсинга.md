# Отладка проблемы со статусом парсинга файлов VI2

## 🎯 **Проблема**

После загрузки файла VI2 статус не изменяется на "Обработан" в UI, хотя:
- ✅ Файл успешно загружается в Supabase Storage
- ✅ Данные успешно парсятся и сохраняются в БД
- ✅ Файл отображается в таблице "Файлы данных логгеров"
- ❌ Статус остается "Не обработан" вместо "Обработан"

## 🔍 **Добавленное логирование**

### **1. В `src/components/QualificationWorkSchedule.tsx`:**

**В функции `loadLoggerRemovalFiles`:**
```typescript
summaries.forEach(summary => {
  const fileKey = getLoggerRemovalFileKey(summary.zone_number, summary.measurement_level);
  console.log(`Загружен статус для ${fileKey}:`, {
    fileKey,
    zone_number: summary.zone_number,
    measurement_level: summary.measurement_level,
    parsing_status: summary.parsing_status,
    record_count: summary.record_count,
    file_name: summary.file_name
  });
  statusMap[fileKey] = summary.parsing_status as 'idle' | 'parsing' | 'completed' | 'error';
  // ...
});
```

**В функции `handleVI2FileParsing`:**
```typescript
// Обновляем статус и результаты
console.log('Устанавливаем локальный статус completed для:', levelId);
setParsingStatus(prev => ({
  ...prev,
  [levelId]: 'completed'
}));

// ...

// Обновляем файлы в Storage после успешной загрузки
console.log('Перезагружаем файлы из Storage и БД...');
await loadLoggerRemovalFiles();
console.log('Перезагрузка завершена');
```

### **2. В `src/utils/loggerDataService.ts`:**

**В функции `saveLoggerData`:**
```typescript
console.log('LoggerDataService: Сохраняем сводную информацию с статусом:', {
  file_name: parsedData.fileName,
  parsing_status: parsedData.parsingStatus,
  record_count: parsedData.recordCount,
  zone_number: zoneNumber,
  measurement_level: measurementLevel
});
```

## 🧪 **Тестирование**

### **Для диагностики проблемы:**

1. **Загрузите файл VI2:**
   - Перейдите в "План-график проведения квалификационных работ"
   - Загрузите файл `.vi2` в любую зону

2. **Проверьте логи в консоли:**

**Ожидаемые логи при загрузке:**
```
Начинаем парсинг VI2 файла: {fileName: "...", zoneNumber: 1, measurementLevel: 0, loggerName: "..."}
Парсинг завершен: {fileName: "...", deviceMetadata: {...}, measurements: [...], ...}
LoggerDataService: Сохраняем сводную информацию с статусом: {file_name: "...", parsing_status: "completed", record_count: 7743, zone_number: 1, measurement_level: 0}
Сводная информация сохранена: {id: "...", project_id: "...", qualification_object_id: "...", zone_number: 1, measurement_level: 0, ...}
Устанавливаем локальный статус completed для: zone-1-level-0
Перезагружаем файлы из Storage и БД...
Загруженные сводки данных логгеров: (8) [{...}, {...}, ...]
Загружен статус для zone-1-level-0: {fileKey: "zone-1-level-0", zone_number: 1, measurement_level: 0, parsing_status: "completed", record_count: 7743, file_name: "..."}
Перезагрузка завершена
```

### **Возможные проблемы:**

1. **Статус в БД не "completed":**
   - Если `parsing_status` в логах не "completed", проблема в парсере
   - Проверить `VI2ParsingService` и бинарные парсеры

2. **Статус в БД "completed", но UI не обновляется:**
   - Если `parsing_status` в БД "completed", но UI показывает "Не обработан"
   - Проблема в логике обновления UI

3. **Конфликт ключей:**
   - Если `fileKey` не совпадает между сохранением и загрузкой
   - Проверить функцию `getLoggerRemovalFileKey`

## 🔧 **Возможные решения**

### **Если статус в БД не "completed":**

1. **Проверить парсеры:**
   - `src/utils/testo174tBinaryParser.ts`
   - `src/utils/testo174hBinaryParser.ts`
   - Убедиться, что возвращают `parsingStatus: 'completed'`

2. **Проверить VI2ParsingService:**
   - Убедиться, что правильно передает статус от парсеров

### **Если статус в БД "completed", но UI не обновляется:**

1. **Проблема с ключами:**
   - Проверить, что `getLoggerRemovalFileKey` генерирует одинаковые ключи
   - Убедиться, что `zone_number` и `measurement_level` совпадают

2. **Проблема с обновлением состояния:**
   - Возможно, `loadLoggerRemovalFiles` перезаписывает локальные изменения
   - Рассмотреть задержку перед вызовом `loadLoggerRemovalFiles`

3. **Проблема с типизацией:**
   - Проверить, что `parsing_status` в БД соответствует типам в UI

## 📊 **Анализ логов**

### **Ключевые моменты для проверки:**

1. **При сохранении:**
   ```
   LoggerDataService: Сохраняем сводную информацию с статусом: {parsing_status: "completed", ...}
   ```

2. **При загрузке:**
   ```
   Загружен статус для zone-1-level-0: {parsing_status: "completed", ...}
   ```

3. **Соответствие ключей:**
   - Ключ при сохранении: `zone-1-level-0`
   - Ключ при загрузке: `zone-1-level-0`

### **Если логи показывают проблему:**

1. **Статус не "completed" в БД:**
   - Исправить парсеры
   - Проверить логику `VI2ParsingService`

2. **Ключи не совпадают:**
   - Исправить `getLoggerRemovalFileKey`
   - Проверить `zone_number` и `measurement_level`

3. **UI не обновляется:**
   - Изменить логику обновления состояния
   - Добавить задержку перед `loadLoggerRemovalFiles`

## 🎯 **Следующие шаги**

1. **Загрузить файл VI2** и проверить логи
2. **Проанализировать** полученные логи
3. **Определить** точную причину проблемы
4. **Применить** соответствующее исправление

## 📋 **Связанные файлы**

- `src/components/QualificationWorkSchedule.tsx` - основная логика UI
- `src/utils/loggerDataService.ts` - сохранение в БД
- `src/utils/vi2Parser.ts` - основной парсер
- `src/utils/testo174tBinaryParser.ts` - парсер одноканальных устройств
- `src/utils/testo174hBinaryParser.ts` - парсер двухканальных устройств

## 🎯 **Заключение**

Добавленное логирование поможет точно определить, на каком этапе теряется статус "completed":
- При парсинге файла
- При сохранении в БД
- При загрузке из БД
- При обновлении UI

**После получения логов можно будет точно исправить проблему!** 🔍



















