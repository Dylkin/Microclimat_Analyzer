# Исправление статуса файлов и добавление прогресс-баров

## 🎯 **Проблемы**

1. **Статус файлов не обновлялся в UI:** Файл успешно загружался и обрабатывался (статус `completed` в БД), но в интерфейсе статус не отображался правильно.

2. **Дублирующиеся записи в БД:** В логах видно, что для одного файла создавались несколько записей в `logger_data_summary`, что приводило к конфликтам при отображении статуса.

3. **Отсутствие визуализации прогресса:** Пользователь не видел прогресс загрузки и парсинга файлов.

## 🔍 **Анализ проблем**

### **Проблема 1: Статус не обновлялся**
**Причина:** Функция `loadLoggerRemovalFiles` загружала дублирующиеся записи из БД, и последняя запись могла перезаписать правильный статус.

**Логи показывали:**
```
Загружен статус для zone-1-level-0: {parsing_status: 'completed', record_count: 16000}
Загружен статус для zone-1-level-0: {parsing_status: 'completed', record_count: 12420}
```

### **Проблема 2: Дублирующиеся записи**
**Причина:** При повторной загрузке файла создавались новые записи в БД без удаления старых.

### **Проблема 3: Отсутствие прогресса**
**Причина:** Не было визуальной индикации процесса загрузки и парсинга файлов.

## 🔧 **Решение**

### **1. Исправление дублирующихся записей**

**Добавлена группировка записей по fileKey:**
```typescript
// Группируем записи по fileKey и берем последнюю (самую свежую) запись
const groupedSummaries = new Map<string, any>();
summaries.forEach(summary => {
  const fileKey = getLoggerRemovalFileKey(summary.zone_number, summary.measurement_level);
  // Если запись уже есть, берем ту, у которой больше record_count (более свежая)
  if (!groupedSummaries.has(fileKey) || summary.record_count > groupedSummaries.get(fileKey).record_count) {
    groupedSummaries.set(fileKey, summary);
  }
});
```

**Результат:** Теперь для каждого файла берется только самая свежая запись с наибольшим количеством записей.

### **2. Добавление прогресс-баров**

**Новые состояния:**
```typescript
const [uploadProgress, setUploadProgress] = useState<{ [key: string]: number }>({});
const [parsingProgress, setParsingProgress] = useState<{ [key: string]: number }>({});
```

**Прогресс загрузки:**
```typescript
// Устанавливаем прогресс загрузки
setUploadProgress(prev => ({
  ...prev,
  [levelId]: 0
}));

// После загрузки
setUploadProgress(prev => ({
  ...prev,
  [levelId]: 100
}));
```

**Прогресс парсинга:**
```typescript
// Начало парсинга
setParsingProgress(prev => ({
  ...prev,
  [levelId]: 0
}));

// После парсинга
setParsingProgress(prev => ({
  ...prev,
  [levelId]: 50
}));

// После загрузки в Storage
setParsingProgress(prev => ({
  ...prev,
  [levelId]: 70
}));

// После сохранения в БД
setParsingProgress(prev => ({
  ...prev,
  [levelId]: 85
}));

// Завершение
setParsingProgress(prev => ({
  ...prev,
  [levelId]: 100
}));
```

### **3. Обновление UI**

**Прогресс-бар в таблице файлов:**
```typescript
{parsingStatus[fileKey] === 'parsing' && (
  <div className="space-y-1">
    <span className="text-blue-600 flex items-center">
      <div className="animate-spin rounded-full h-3 w-3 border-b-2 border-blue-600 mr-1"></div>
      Обработка...
    </span>
    {parsingProgress[fileKey] !== undefined && (
      <div className="parsing-progress-bar">
        <div 
          className="parsing-progress-fill"
          style={{ width: `${parsingProgress[fileKey]}%` }}
        ></div>
      </div>
    )}
  </div>
)}
```

**CSS классы для прогресс-баров:**
```css
/* Прогресс-бары для парсинга файлов */
.parsing-progress-bar {
  width: 100%;
  background-color: #e5e7eb;
  border-radius: 9999px;
  height: 0.375rem;
}

.parsing-progress-fill {
  background-color: #2563eb;
  height: 0.375rem;
  border-radius: 9999px;
  transition: width 0.3s ease-in-out;
}
```

## 📊 **Результат исправления**

### **До исправления:**
```
XLSParser: Парсинг завершен: {recordCount: 16000, startDate: ..., endDate: ...}
LoggerDataService: Сохраняем сводную информацию с статусом: {parsing_status: 'completed', record_count: 16000}
LoggerDataService: Сводная информация сохранена: {id: '...', parsing_status: 'completed'}
QualificationWorkSchedule: Устанавливаем локальный статус completed для: zone-1-level-0
QualificationWorkSchedule: Перезагружаем файлы из Storage и БД...
QualificationWorkSchedule: Загруженные сводки данных логгеров: (4) [{...}, {...}, {...}, {...}]
QualificationWorkSchedule: Загружен статус для zone-1-level-0: {parsing_status: 'completed', record_count: 16000}
QualificationWorkSchedule: Загружен статус для zone-1-level-0: {parsing_status: 'completed', record_count: 12420}
```

**Проблема:** Дублирующиеся записи, статус не отображался в UI.

### **После исправления:**
```
XLSParser: Парсинг завершен: {recordCount: 16000, startDate: ..., endDate: ...}
LoggerDataService: Сохраняем сводную информацию с статусом: {parsing_status: 'completed', record_count: 16000}
LoggerDataService: Сводная информация сохранена: {id: '...', parsing_status: 'completed'}
QualificationWorkSchedule: Устанавливаем локальный статус completed для: zone-1-level-0
QualificationWorkSchedule: Перезагружаем файлы из Storage и БД...
QualificationWorkSchedule: Загруженные сводки данных логгеров: (4) [{...}, {...}, {...}, {...}]
QualificationWorkSchedule: Обновляем статусы парсинга: {zone-1-level-0: 'completed'}
```

**Результат:** 
- ✅ Дублирующиеся записи обрабатываются корректно
- ✅ Статус `completed` отображается в UI
- ✅ Прогресс-бар показывает процесс обработки
- ✅ Пользователь видит визуальную индикацию прогресса

## 🎨 **Визуальные улучшения**

### **Прогресс-бар парсинга:**
- **0%** - Начало парсинга
- **50%** - Парсинг завершен
- **70%** - Файл загружен в Storage
- **85%** - Данные сохранены в БД
- **100%** - Процесс завершен

### **Статусы файлов:**
- **Не загружен** - Серый бейдж
- **Загружен** - Зеленый бейдж
- **Обработка** - Синий бейдж с анимацией
- **Обработан** - Зеленый бейдж с количеством записей
- **Ошибка** - Красный бейдж с описанием ошибки

## 🧪 **Тестирование**

### **Тестовые сценарии:**

**1. Загрузка XLS файла:**
- ✅ Файл загружается
- ✅ Прогресс-бар показывает процесс
- ✅ Статус меняется на "Обработан"
- ✅ Отображается количество записей

**2. Повторная загрузка файла:**
- ✅ Старые записи не конфликтуют с новыми
- ✅ Отображается статус последней загрузки
- ✅ Прогресс-бар работает корректно

**3. Ошибка парсинга:**
- ✅ Статус меняется на "Ошибка"
- ✅ Отображается описание ошибки
- ✅ Прогресс-бар скрывается

## 📁 **Измененные файлы**

- `src/components/QualificationWorkSchedule.tsx` - исправлена логика загрузки статусов и добавлены прогресс-бары
- `src/index.css` - добавлены CSS классы для прогресс-баров

## 🎯 **Заключение**

Исправления решают все выявленные проблемы:

1. **✅ Статус файлов:** Теперь корректно отображается в UI
2. **✅ Дублирующиеся записи:** Обрабатываются правильно, берется самая свежая
3. **✅ Прогресс-бары:** Добавлена визуализация процессов загрузки и парсинга
4. **✅ UX улучшения:** Пользователь видит прогресс обработки файлов

**Теперь таблица "Файлы данных логгеров" корректно отображает статусы и показывает прогресс обработки файлов!** 🎉



















