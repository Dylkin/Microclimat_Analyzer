# Перенос блока "Реквизиты договора" в блок "Договор"

## 🎯 **Задача**

Перенести блок "Реквизиты договора" внутрь блока "Договор" в компоненте согласования документов для улучшения логической группировки связанных элементов.

## 🔧 **Выполненные изменения**

### **1. Создание специальной функции для рендеринга блока договора**

**В файле `src/components/contract/DocumentApproval.tsx`:**

**Добавлена новая функция `renderContractSection`:**
```typescript
// Специальная функция для рендеринга блока договора с реквизитами
const renderContractSection = (
  title: string,
  document?: ProjectDocument,
  documentType: 'contract' = 'contract'
) => {
  // Получаем статус из базы данных
  const dbStatus = document ? documentStatuses.get(document.id) : undefined;
  const isApproved = document ? approvedDocuments.has(document.id) : false;
  
  // Определяем текущий статус: приоритет у данных из базы, затем у локального состояния
  const currentStatus = dbStatus?.status || (isApproved ? 'approved' : 'pending');
  
  console.log(`DocumentApproval: Статус для ${title} (${document?.id}):`, {
    dbStatus: dbStatus?.status,
    isApproved,
    currentStatus,
    hasDocument: !!document
  });

  return (
    <div className="bg-gray-50 rounded-lg p-4 border border-gray-200">
      <div className="flex items-center justify-between mb-4">
        <div className="flex items-center space-x-3">
          {getStatusIcon(!!document, currentStatus === 'approved')}
          <h4 className="font-medium text-gray-900">{title}</h4>
        </div>
        <span className={`text-sm px-2 py-1 rounded-full ${getStatusColor(!!document, currentStatus === 'approved')}`}>
          {getStatusText(!!document, currentStatus === 'approved')}
        </span>
      </div>

      {/* Секция документа */}
      {document ? (
        <div className="space-y-3">
          {/* Document info */}
          <div className="flex items-center justify-between p-3 bg-white rounded border">
            <div className="flex items-center space-x-3">
              <FileText className="w-5 h-5 text-blue-500" />
              <div>
                <p className="text-sm font-medium text-gray-900">{document.fileName}</p>
                <p className="text-xs text-gray-500">
                  Загружен: {new Date(document.uploadedAt).toLocaleDateString('ru-RU')}
                </p>
              </div>
            </div>
            <div className="flex items-center space-x-2">
              <a
                href={document.fileUrl}
                target="_blank"
                rel="noopener noreferrer"
                className="text-blue-600 hover:text-blue-800"
                title="Просмотреть документ"
              >
                <Eye className="w-4 h-4" />
              </a>
              {!isApproved && (
                <button
                  onClick={() => onDelete(document.id)}
                  className="text-red-600 hover:text-red-800"
                  title="Удалить документ"
                >
                  <Trash2 className="w-4 h-4" />
                </button>
              )}
            </div>
          </div>
        </div>
      ) : (
        <div className="text-center py-4">
          <FileText className="w-8 h-8 text-gray-400 mx-auto mb-2" />
          <p className="text-sm text-gray-500 mb-3">Документ не загружен</p>
          <label className="inline-flex items-center px-3 py-1 bg-indigo-600 text-white text-sm rounded hover:bg-indigo-700 transition-colors cursor-pointer">
            <Upload className="w-4 h-4 mr-1" />
            Загрузить документ
            <input
              type="file"
              accept=".pdf,.doc,.docx"
              onChange={(e) => handleFileSelect(documentType, e)}
              className="hidden"
            />
          </label>
        </div>
      )}

      {/* Реквизиты договора - интегрированы в блок договора */}
      {project && (
        <div className="mt-4 pt-4 border-t border-gray-200">
          <ContractFields
            project={project}
            onUpdate={onProjectUpdate || (() => {})}
            isEditing={isEditingContractFields}
            onEditToggle={setIsEditingContractFields}
          />
        </div>
      )}

      {/* Comments - Always shown below document upload section */}
      <div className="mt-4">
        <DocumentComments
          documentId={document?.id || `temp-${documentType}`}
          documentType={documentType}
          onCommentAdd={(_comment) => {
            console.log('Comment added:', _comment);
          }}
        />
      </div>

      {/* Approval Actions - Moved below comments */}
      {document && (
        <div className="mt-4">
          <DocumentApprovalActions
            documentId={document.id}
            documentType={documentType}
            currentStatus={currentStatus}
            isCancelBlocked={isApprovalCancelBlocked(documentType)}
            onStatusChange={(status, _comment) => {
              if (status === 'approved') {
                handleDocumentApproval(document.id);
              } else if (status === 'pending') {
                // Отмена согласования
                onUnapprove(document.id);
              } else if (status === 'rejected') {
                // Логика отклонения
                console.log('Document rejected:', document.id, _comment);
              }
              // Обновляем статусы после изменения
              loadDocumentStatuses();
            }}
          />
        </div>
      )}
    </div>
  );
};
```

### **2. Обновление вызова функции рендеринга**

**До изменения:**
```typescript
<div className="space-y-6">
  {renderDocumentSection('Коммерческое предложение', commercialOfferDoc, 'commercial_offer')}
  {renderDocumentSection('Договор', contractDoc, 'contract')}
  
  {/* Реквизиты договора */}
  {project && (
    <ContractFields
      project={project}
      onUpdate={onProjectUpdate || (() => {})}
      isEditing={isEditingContractFields}
      onEditToggle={setIsEditingContractFields}
    />
  )}
  
  {/* Протоколы квалификации */}
  // ...
</div>
```

**После изменения:**
```typescript
<div className="space-y-6">
  {renderDocumentSection('Коммерческое предложение', commercialOfferDoc, 'commercial_offer')}
  {renderContractSection('Договор', contractDoc, 'contract')}
  
  {/* Протоколы квалификации */}
  // ...
</div>
```

## 🔄 **Что изменилось**

### **До изменения:**
- ❌ Блок "Реквизиты договора" был отдельным элементом
- ❌ Логически несвязанные элементы были разбросаны
- ❌ Дублирование логики рендеринга
- ❌ Неоптимальная структура интерфейса

### **После изменения:**
- ✅ Блок "Реквизиты договора" интегрирован в блок "Договор"
- ✅ Логически связанные элементы сгруппированы
- ✅ Специализированная функция для договора
- ✅ Улучшенная структура интерфейса

## 📊 **Новая структура блока "Договор"**

### **Иерархия элементов:**
```
Блок "Договор"
├── Заголовок с иконкой статуса
├── Информация о документе (если загружен)
│   ├── Название файла
│   ├── Дата загрузки
│   ├── Кнопка просмотра
│   └── Кнопка удаления (если не согласован)
├── Загрузка документа (если не загружен)
├── Реквизиты договора (интегрированы)
│   ├── Номер договора
│   ├── Дата договора
│   └── Кнопки редактирования
├── Комментарии
└── Действия согласования
    ├── Кнопка "Согласовано"
    └── Кнопка "Отменить согласование"
```

## 🎯 **Преимущества изменения**

### **1. 🎨 Улучшенная логическая группировка:**
- Все элементы, связанные с договором, находятся в одном блоке
- Пользователь видит полную картину по договору в одном месте
- Уменьшение когнитивной нагрузки

### **2. 📱 Более интуитивный интерфейс:**
- Логическая связь между документом и его реквизитами
- Единый контекст для всех операций с договором
- Улучшенная навигация по интерфейсу

### **3. 🔧 Техническая оптимизация:**
- Специализированная функция для договора
- Переиспользование логики рендеринга
- Более чистый и поддерживаемый код

### **4. 💡 Пользовательский опыт:**
- Все действия с договором в одном месте
- Понятная структура интерфейса
- Уменьшение количества отдельных блоков

## 🧪 **Тестирование**

### **Для проверки изменений:**

1. **Проверьте структуру блока "Договор":**
   - Откройте страницу "Согласование договора"
   - Убедитесь, что блок "Договор" содержит реквизиты
   - Проверьте, что реквизиты отображаются внутри блока договора

2. **Проверьте функциональность:**
   - Убедитесь, что загрузка документа работает
   - Проверьте, что реквизиты можно редактировать
   - Убедитесь, что согласование работает корректно

3. **Проверьте визуальное оформление:**
   - Убедитесь, что реквизиты отделены разделителем
   - Проверьте, что все элементы выровнены правильно
   - Убедитесь, что интерфейс выглядит аккуратно

4. **Проверьте отсутствие дублирования:**
   - Убедитесь, что блок "Реквизиты договора" больше не отображается отдельно
   - Проверьте, что нет дублирования функциональности

### **Ожидаемое поведение:**

**Структура блока "Договор":**
- ✅ Заголовок с иконкой статуса
- ✅ Информация о документе или загрузка
- ✅ Реквизиты договора внутри блока
- ✅ Комментарии и действия согласования

**Функциональность:**
- ✅ Все функции работают как раньше
- ✅ Реквизиты редактируются корректно
- ✅ Согласование работает правильно
- ✅ Нет дублирования элементов

**Визуальное оформление:**
- ✅ Логичная группировка элементов
- ✅ Четкие разделители между секциями
- ✅ Единообразный стиль оформления

## 📋 **Связанные файлы**

- `src/components/contract/DocumentApproval.tsx` - основная логика с новой функцией `renderContractSection`

## 🎯 **Заключение**

Перенос блока "Реквизиты договора" в блок "Договор" полностью выполнен:

- **Логическая группировка:** Все элементы договора объединены в одном блоке
- **Улучшенный UX:** Более интуитивная структура интерфейса
- **Техническая оптимизация:** Специализированная функция рендеринга
- **Чистота кода:** Устранение дублирования и улучшение структуры

**Результат: блок "Договор" теперь содержит все связанные элементы, включая реквизиты!** 🎉



















