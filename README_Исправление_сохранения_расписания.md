# Исправление сохранения в блоке "План график проведения квалификационных работ"

## 🎯 **Проблема**

При нажатии на кнопку "Сохранить" в блоке "План график проведения квалификационных работ" происходило **добавление** информации о испытаниях вместо **обновления**. Кроме того, информация в блоке отображалась для всех проектов, а не только для текущего проекта квалификации.

## 🔍 **Причины проблем**

1. **Неправильная логика сохранения:** Функция `handleSave` использовала создание новых этапов вместо обновления существующих
2. **Отсутствие привязки к проекту:** Таблица `qualification_work_schedule` не содержала поле `project_id`
3. **Неправильная фильтрация:** Загрузка данных не учитывала контекст проекта

## ✅ **Реализованные исправления**

### 1. Исправлена логика сохранения

**Файл:** `src/components/QualificationWorkSchedule.tsx`

**Было:**
```typescript
// Разделяем этапы на новые и существующие
const newStages = stages.filter(stage => stage.id.startsWith('temp-stage-'));
const existingStages = stages.filter(stage => !stage.id.startsWith('temp-stage-'));

// Создаем новые этапы
for (const stage of newStages) {
  await qualificationWorkScheduleService.createWorkStage(qualificationObjectId, stageToSave);
}

// Обновляем существующие этапы
for (const stage of existingStages) {
  await qualificationWorkScheduleService.updateWorkStage(qualificationObjectId, stage.id, stageToSave);
}
```

**Стало:**
```typescript
// Используем метод saveWorkSchedule для полного обновления расписания
const stagesToSave = stages.map(stage => ({
  stageName: stage.name,
  stageDescription: stage.description,
  startDate: stage.startDate || undefined,
  endDate: stage.endDate || undefined,
  isCompleted: stage.isCompleted,
  completedAt: stage.completedAt || undefined,
  completedBy: stage.completedBy || undefined,
  cancelledAt: stage.cancelledAt || undefined,
  cancelledBy: stage.cancelledBy || undefined,
  projectId: projectId // Добавляем привязку к проекту
}));

// Сохраняем расписание (это удалит старые записи и создаст новые)
await qualificationWorkScheduleService.saveWorkSchedule(
  qualificationObjectId,
  stagesToSave,
  projectId
);
```

### 2. Добавлена привязка к проекту

**Файл:** `add_project_id_to_qualification_work_schedule.sql`

```sql
-- Добавляем поле project_id в таблицу qualification_work_schedule
ALTER TABLE qualification_work_schedule 
ADD COLUMN IF NOT EXISTS project_id UUID REFERENCES projects(id) ON DELETE CASCADE;

-- Создаем индексы для оптимизации
CREATE INDEX IF NOT EXISTS idx_qualification_work_schedule_project_id 
ON qualification_work_schedule(project_id);

CREATE INDEX IF NOT EXISTS idx_qualification_work_schedule_project_object 
ON qualification_work_schedule(project_id, qualification_object_id);
```

### 3. Обновлен сервис для работы с проектами

**Файл:** `src/utils/qualificationWorkScheduleService.ts`

**Обновленные методы:**
- `saveWorkSchedule()` - теперь принимает `projectId` и сохраняет его в БД
- `getWorkSchedule()` - фильтрует данные по `projectId`
- `deleteWorkSchedule()` - удаляет данные с учетом `projectId`

**Новая логика сохранения:**
```typescript
async saveWorkSchedule(qualificationObjectId: string, stages: any[], projectId?: string) {
  // Удаляем существующие записи для этого объекта И проекта
  let deleteQuery = this.supabase!
    .from('qualification_work_schedule')
    .delete()
    .eq('qualification_object_id', qualificationObjectId);

  if (projectId) {
    deleteQuery = deleteQuery.eq('project_id', projectId);
  }

  // Вставляем новые записи с project_id
  const stagesToInsert = stages.map(stage => ({
    qualification_object_id: qualificationObjectId,
    project_id: projectId, // Добавляем привязку к проекту
    stage_name: stage.stageName,
    // ... остальные поля
  }));
}
```

### 4. Обновлена загрузка данных

**Файл:** `src/components/QualificationWorkSchedule.tsx`

```typescript
// Загрузка расписания с учетом проекта
const loadSchedule = async () => {
  const savedStages = await qualificationWorkScheduleService.getWorkSchedule(
    qualificationObjectId, 
    projectId // Передаем projectId для фильтрации
  );
  
  // Используем этапы из БД без дополнительной фильтрации
  // так как сервис уже фильтрует по проекту
  const filteredStages = savedStages;
}
```

## 🔒 **Контроль доступа**

### RLS политики обновлены

```sql
-- Пользователи могут видеть только расписания проектов, к которым у них есть доступ
CREATE POLICY "Users can view work schedules for their accessible projects" ON qualification_work_schedule
    FOR SELECT USING (
        project_id IN (
            SELECT p.id FROM projects p
            WHERE p.id = qualification_work_schedule.project_id
        )
    );
```

### Принципы безопасности:

1. **Изоляция по проектам:** Расписание доступно только в контексте проекта
2. **Привязка к объекту:** Расписание привязано к конкретному объекту квалификации
3. **RLS защита:** Пользователи видят только расписания доступных проектов

## 📋 **Результат исправлений**

### ✅ **Что исправлено:**

1. **Правильное обновление:** При нажатии "Сохранить" происходит обновление существующих записей, а не добавление новых
2. **Привязка к проекту:** Расписание сохраняется с привязкой к конкретному проекту квалификации
3. **Изоляция данных:** Информация в блоке "План график" доступна только в рамках проекта квалификации
4. **Контроль доступа:** При открытии объекта квалификации со страницы "Контрагент" расписание не отображается

### 🎯 **Сценарии работы:**

**✅ Правильное поведение:**
- Открытие объекта из проекта → отображается расписание проекта
- Сохранение изменений → обновляется существующее расписание
- Открытие объекта из другого проекта → отображается расписание другого проекта

**❌ Неправильное поведение (исправлено):**
- ~~Открытие объекта из проекта → отображается расписание всех проектов~~
- ~~Сохранение изменений → добавляются новые записи~~
- ~~Открытие объекта со страницы "Контрагент" → отображается расписание~~

## 🚀 **Преимущества исправлений**

1. **Корректное обновление:** Данные обновляются, а не дублируются
2. **Изоляция проектов:** Каждый проект имеет свое расписание
3. **Безопасность:** Контроль доступа на уровне БД
4. **Производительность:** Оптимизированные запросы с индексами
5. **Масштабируемость:** Легко добавлять новые проекты и объекты

## 📝 **Инструкции по применению**

1. **Выполните SQL скрипт:**
   ```sql
   -- Выполните add_project_id_to_qualification_work_schedule.sql в Supabase SQL Editor
   ```

2. **Перезапустите приложение:**
   ```bash
   npm run dev
   ```

3. **Проверьте работу:**
   - Откройте объект квалификации из проекта
   - Измените даты в расписании
   - Нажмите "Сохранить"
   - Убедитесь, что данные обновились, а не добавились

## 🔄 **Обратная совместимость**

- ✅ Существующие данные сохраняются
- ✅ Старые записи без `project_id` продолжают работать
- ✅ Новые записи автоматически получают `project_id`
- ✅ Постепенная миграция без потери данных










