# Отладка параметра isCheckboxesBlocked

## 🎯 **Цель**

Проверить текущее значение параметра `isCheckboxesBlocked` для существующего проекта квалификации и понять логику его определения.

## 🔧 **Добавленная отладочная информация**

### **1. В компоненте ContractNegotiation.tsx**

Добавлен console.log для отслеживания логики определения `isCheckboxesBlocked`:

```typescript
isCheckboxesBlocked={(() => {
  const contractDoc = documents.find(doc => doc.documentType === 'contract');
  const isBlocked = contractDoc ? approvedDocuments.has(contractDoc.id) : false;
  console.log('🔒 isCheckboxesBlocked debug:', {
    contractDoc: contractDoc ? { id: contractDoc.id, documentType: contractDoc.documentType } : null,
    approvedDocuments: Array.from(approvedDocuments),
    isBlocked
  });
  return isBlocked;
})()}
```

### **2. В компоненте QualificationObjectsCRUD.tsx**

Добавлен console.log для отслеживания получаемого значения:

```typescript
// Отладочная информация для isCheckboxesBlocked
console.log('🔒 QualificationObjectsCRUD received isCheckboxesBlocked:', isCheckboxesBlocked);
```

## 📋 **Логика определения isCheckboxesBlocked**

### **Условие блокировки:**
```typescript
const contractDoc = documents.find(doc => doc.documentType === 'contract');
const isBlocked = contractDoc ? approvedDocuments.has(contractDoc.id) : false;
```

### **Логика работы:**
1. **Ищется документ типа 'contract'** в массиве `documents`
2. **Если документ найден** - проверяется, есть ли его ID в `approvedDocuments`
3. **Если документ не найден** - возвращается `false` (не заблокировано)

### **Состояния:**
- **`isCheckboxesBlocked = false`** - объекты квалификации доступны для редактирования
- **`isCheckboxesBlocked = true`** - объекты квалификации заблокированы для редактирования

## 🧪 **Как проверить значение**

### **Шаг 1: Откройте DevTools**
1. Откройте страницу "Согласование договора"
2. Нажмите F12 или Ctrl+Shift+I
3. Перейдите на вкладку "Console"

### **Шаг 2: Найдите отладочные сообщения**
Ищите сообщения с префиксом `🔒`:

```
🔒 isCheckboxesBlocked debug: {
  contractDoc: { id: "uuid-here", documentType: "contract" },
  approvedDocuments: ["uuid-here"],
  isBlocked: true
}
```

```
🔒 QualificationObjectsCRUD received isCheckboxesBlocked: true
```

### **Шаг 3: Анализ результатов**

#### **Если `isBlocked: false`:**
- ✅ Объекты квалификации доступны для редактирования
- ✅ Кнопки "Просмотр" и "Выполнить" активны
- ✅ Чекбоксы интерактивные

#### **Если `isBlocked: true`:**
- ❌ Объекты квалификации заблокированы
- ❌ Кнопки "Просмотр" и "Выполнить" неактивны
- ❌ Чекбоксы в режиме просмотра

## 📊 **Возможные состояния**

### **1. Договор не загружен:**
```javascript
{
  contractDoc: null,
  approvedDocuments: [],
  isBlocked: false
}
```

### **2. Договор загружен, но не согласован:**
```javascript
{
  contractDoc: { id: "uuid-123", documentType: "contract" },
  approvedDocuments: [],
  isBlocked: false
}
```

### **3. Договор загружен и согласован:**
```javascript
{
  contractDoc: { id: "uuid-123", documentType: "contract" },
  approvedDocuments: ["uuid-123"],
  isBlocked: true
}
```

## 🔍 **Диагностика проблем**

### **Проблема: Блокировка не работает**
**Возможные причины:**
1. `contractDoc` равен `null` - договор не загружен
2. `approvedDocuments` не содержит ID договора - договор не согласован
3. Логика определения работает неправильно

### **Проблема: Блокировка работает неправильно**
**Возможные причины:**
1. `approvedDocuments` содержит неправильные ID
2. Состояние `approvedDocuments` не обновляется
3. Компонент не получает правильное значение

## 🛠️ **Команды для проверки**

### **В консоли браузера:**
```javascript
// Проверить состояние documents
console.log('Documents:', window.documents);

// Проверить состояние approvedDocuments
console.log('Approved Documents:', window.approvedDocuments);

// Проверить конкретный договор
const contractDoc = documents.find(doc => doc.documentType === 'contract');
console.log('Contract Document:', contractDoc);
```

## 📝 **Ожидаемые результаты**

### **Для нового проекта:**
- `isCheckboxesBlocked: false`
- Объекты квалификации доступны

### **Для проекта с согласованным договором:**
- `isCheckboxesBlocked: true`
- Объекты квалификации заблокированы

### **Для проекта с загруженным, но не согласованным договором:**
- `isCheckboxesBlocked: false`
- Объекты квалификации доступны

## 🎯 **Следующие шаги**

После проверки значения `isCheckboxesBlocked`:

1. **Если значение правильное** - проблема в логике блокировки
2. **Если значение неправильное** - проблема в определении состояния
3. **Если значение не отображается** - проблема в загрузке компонента

**Отладочная информация поможет точно определить, где находится проблема!** 🔍




