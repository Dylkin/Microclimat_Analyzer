# Диагностика источника проблемы с полями договора

## 🔍 **Проблема диагностирована:**

Из логов видно, что данные **теряются уже в App.tsx**:

```javascript
App: handlePageChange вызван: {page: 'contract_negotiation', projectData: {…}}
App: Рендеринг contract_negotiation с проектом: {selectedProject: {…}}
```

**НО** в `ContractNegotiation` данные уже `undefined`:

```javascript
ContractNegotiation: Получен проект: {
  contractNumber: undefined,    // ❌ Уже undefined!
  contractDate: undefined,      // ❌ Уже undefined!
}
```

## 🛠️ **Добавлено расширенное логирование:**

### 1. В App.tsx
- Добавлен `fullProject` в логи для полной диагностики объекта

### 2. В projectService.getAllProjects()
- Добавлено логирование маппинга конкретного проекта PharmDistri
- Показывает исходные данные из БД и результат маппинга

## 🧪 **Тестирование:**

### Шаг 1: Выполните SQL скрипт
Выполните `check_project_contract_data.sql` в Supabase SQL Editor для проверки данных в БД.

### Шаг 2: Очистите консоль и перезагрузите страницу
1. Очистите консоль браузера (Ctrl+L)
2. Перезагрузите страницу (F5)
3. Перейдите на страницу "Согласование договора"

### Шаг 3: Проверьте логи в консоли

**Ожидаемые логи (в правильном порядке):**

```javascript
// 1. Загрузка проектов
ProjectService: getAllProjects - маппинг проекта PharmDistri: {
  rawProject: {
    id: "36b01543-0e89-4954-956a-77a370f78954",
    name: "PharmDistri - WW Transporter, ХК №1",
    contract_number: "ВР-15-25",        // ← Должно быть не null
    contract_date: "2025-10-01"        // ← Должно быть не null
  },
  mappedProject: {
    id: "36b01543-0e89-4954-956a-77a370f78954",
    name: "PharmDistri - WW Transporter, ХК №1",
    contractNumber: "ВР-15-25",        // ← Должно быть не undefined
    contractDate: Date object          // ← Должно быть не undefined
  }
}

// 2. Навигация
App: handlePageChange вызван: {
  page: "contract_negotiation",
  projectData: {
    id: "36b01543-0e89-4954-956a-77a370f78954",
    name: "PharmDistri - WW Transporter, ХК №1",
    contractNumber: "ВР-15-25",        // ← Должно быть не undefined
    contractDate: "2025-10-01",        // ← Должно быть не undefined
    fullProject: { ... }               // ← Полный объект для диагностики
  }
}

// 3. Рендеринг
App: Рендеринг contract_negotiation с проектом: {
  selectedProject: {
    id: "36b01543-0e89-4954-956a-77a370f78954",
    name: "PharmDistri - WW Transporter, ХК №1",
    contractNumber: "ВР-15-25",        // ← Должно быть не undefined
    contractDate: "2025-10-01",        // ← Должно быть не undefined
    fullProject: { ... }               // ← Полный объект для диагностики
  }
}

// 4. Инициализация ContractNegotiation
ContractNegotiation: Получен проект: {
  id: "36b01543-0e89-4954-956a-77a370f78954",
  name: "PharmDistri - WW Transporter, ХК №1",
  contractNumber: "ВР-15-25",          // ← Должно быть не undefined
  contractDate: "2025-10-01",          // ← Должно быть не undefined
  project: { ... }
}
```

## 🚨 **Возможные сценарии:**

### Сценарий 1: Данные в БД отсутствуют
**Симптом**: В логах `ProjectService: getAllProjects` показывают `null` для `contract_number` и `contract_date`
**Решение**: Выполните SQL скрипт для обновления данных в БД

### Сценарий 2: Данные теряются при маппинге
**Симптом**: В БД данные есть, но в `mappedProject` они `undefined`
**Решение**: Проблема в логике маппинга в `projectService.getAllProjects()`

### Сценарий 3: Данные теряются в App.tsx
**Симптом**: В `projectService` данные правильные, но в `App.tsx` они `undefined`
**Решение**: Проблема в передаче данных между компонентами

### Сценарий 4: Данные теряются в ContractNegotiation
**Симптом**: В `App.tsx` данные правильные, но в `ContractNegotiation` они `undefined`
**Решение**: Проблема в передаче данных из `App.tsx` в `ContractNegotiation`

## 📋 **Что нужно сообщить:**

После тестирования сообщите:

1. **Результат SQL скрипта** - есть ли данные в БД
2. **Логи из projectService** - правильно ли работает маппинг
3. **Логи из App.tsx** - правильно ли передаются данные
4. **Логи из ContractNegotiation** - правильно ли получаются данные

## 🎯 **Ожидаемый результат:**

После исправления все логи должны показывать правильные данные:
- `contractNumber: "ВР-15-25"` (не `undefined`)
- `contractDate: Date object` (не `undefined`)

И данные должны отображаться в интерфейсе.



















