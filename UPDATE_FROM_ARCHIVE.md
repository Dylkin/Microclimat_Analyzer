# 📦 Обновление продакшена из архива

Альтернативный способ обновления продакшена без использования Git. Используется для развертывания собранного проекта из Dev окружения.

## 📋 Быстрый старт

### Шаг 1: Создание архива в Dev окружении

#### Linux/macOS:
```bash
# Сделать скрипт исполняемым (один раз)
chmod +x create-build-archive.sh

# Создать архив
./create-build-archive.sh [директория_проекта] [директория_вывода]
```

#### Windows (PowerShell):
```powershell
# Создать архив
.\create-build-archive.ps1 -ProjectDir "C:\Microclimat_Analyzer" -OutputDir "C:\Builds"
```

Архив будет создан в указанной директории с именем `microclimat-build-YYYYMMDD_HHMMSS.tar.gz` (Linux) или `.zip` (Windows).

### Шаг 2: Копирование архива на продакшен сервер

```bash
# Пример копирования через SCP
scp microclimat-build-20241130_120000.tar.gz stas@192.168.98.42:/tmp/
```

### Шаг 3: Обновление продакшена

#### Linux/macOS:
```bash
# Сделать скрипт исполняемым (один раз)
chmod +x update-prod-from-archive.sh

# Запустить обновление
./update-prod-from-archive.sh /tmp/microclimat-build-20241130_120000.tar.gz /home/stas/Microclimat_Analyzer
```

#### Windows (PowerShell):
```powershell
# Запустить обновление
.\update-prod-from-archive.ps1 -ArchivePath "C:\Builds\microclimat-build-20241130_120000.zip" -ProjectDir "C:\Microclimat_Analyzer"
```

## 📝 Что делает скрипт

1. ✅ **Создание резервной копии**
   - Сохраняет текущее состояние проекта
   - Исключает `node_modules`, `dist`, `.git`

2. ✅ **Распаковка архива**
   - Поддерживает форматы: `.tar.gz`, `.tgz`, `.zip`
   - Автоматически определяет структуру архива

3. ✅ **Установка зависимостей**
   - Выполняет `npm install`
   - Устанавливает все необходимые пакеты

4. ✅ **Обновление базы данных**
   - Применяет миграции через `npm run setup-db`
   - Обновляет структуру таблиц

5. ✅ **Использование/сборка фронтенда**
   - Если в архиве есть `dist`, использует его
   - Если `dist` нет, выполняет сборку

6. ✅ **Перезапуск сервисов**
   - PM2 (если используется)
   - Systemd сервисы
   - Nginx/IIS

7. ✅ **Проверка работоспособности**
   - Проверяет наличие файлов сборки
   - Проверяет статус сервисов

## 🔧 Параметры скриптов

### `create-build-archive.sh` / `create-build-archive.ps1`

- `[директория_проекта]` - Путь к проекту (по умолчанию: текущая директория)
- `[директория_вывода]` - Куда сохранить архив (по умолчанию: текущая директория)

### `update-prod-from-archive.sh` / `update-prod-from-archive.ps1`

- `<путь_к_архиву>` - Обязательный параметр. Путь к архиву с собранным проектом
- `[директория_проекта]` - Путь к директории проекта на продакшене (по умолчанию: `/home/stas/Microclimat_Analyzer` для Linux, `C:\Microclimat_Analyzer` для Windows)

## 📦 Что входит в архив

Архив содержит:
- `package.json` и `package-lock.json` - зависимости
- `server/` - серверный код
- `src/` - исходный код фронтенда
- `dist/` - собранный фронтенд (опционально, если уже собран)
- `database_setup.sql` - скрипт инициализации БД
- `supabase/` - миграции базы данных
- Конфигурационные файлы (`tsconfig.json`, `vite.config.ts`, `.env.example`)

## ⚠️ Важные замечания

1. **Архив должен содержать собранный `dist/`** для быстрого развертывания. Если `dist/` отсутствует, скрипт попытается собрать проект на продакшене.

2. **Файл `.env` не включается в архив** по соображениям безопасности. Убедитесь, что на продакшене есть правильный `.env` файл.

3. **Резервная копия создается автоматически** перед обновлением. Проверьте директорию `backups/` в проекте.

4. **База данных обновляется автоматически** через `npm run setup-db`. Убедитесь, что в `.env` указаны правильные параметры подключения к БД.

## 🔄 Сравнение с обновлением из Git

| Параметр | Из Git | Из архива |
|----------|--------|-----------|
| Требуется Git | ✅ Да | ❌ Нет |
| Требуется интернет | ✅ Да | ❌ Нет |
| Скорость развертывания | ⚡ Быстро | ⚡ Быстро |
| Контроль версий | ✅ Да | ⚠️ Через имя архива |
| Откат изменений | ✅ Легко | ⚠️ Через резервную копию |

## 🚀 Пример полного процесса

```bash
# 1. В Dev окружении
cd /path/to/Microclimat_Analyzer
./create-build-archive.sh . /tmp/builds

# 2. Копирование на продакшен
scp /tmp/builds/microclimat-build-*.tar.gz stas@192.168.98.42:/tmp/

# 3. На продакшене
ssh stas@192.168.98.42
cd /home/stas/Microclimat_Analyzer
./update-prod-from-archive.sh /tmp/microclimat-build-*.tar.gz
```

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи в консоли выполнения скрипта
- Резервные копии в `backups/`
- Статус сервисов: `pm2 list`, `systemctl status nginx`

