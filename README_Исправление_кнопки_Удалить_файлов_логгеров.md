# Исправление кнопки "Удалить" в таблице "Файлы данных логгеров"

## 🎯 **Проблема**

Кнопка "Удалить" в таблице "Файлы данных логгеров" некорректно работала:
- Файлы удалялись только из локального состояния UI
- Файлы не удалялись из Supabase Storage
- Данные не удалялись из базы данных
- Информация об удаленных файлах отображалась только после перезагрузки страницы

## 🔧 **Выполненные изменения**

### **1. Добавлен метод удаления файлов из Storage в `src/utils/qualificationObjectService.ts`:**

**Новый метод `deleteLoggerRemovalFile`:**
```typescript
// Удаление файла снятия логгеров из Storage
async deleteLoggerRemovalFile(objectId: string, zoneNumber: number, level: number): Promise<void> {
  if (!this.isAvailable()) {
    throw new Error('Supabase не настроен');
  }

  try {
    // Получаем список файлов в папке для данной зоны и уровня
    const folderPath = `logger-removal/${objectId}/zone-${zoneNumber}-level-${level}`;
    const { data: files, error: listError } = await this.supabase!.storage
      .from('qualification-objects')
      .list(folderPath);

    if (listError) {
      console.error('Ошибка получения списка файлов для удаления:', listError);
      return;
    }

    if (files && files.length > 0) {
      // Удаляем все файлы в папке
      const filePaths = files.map(file => `${folderPath}/${file.name}`);
      const { error: deleteError } = await this.supabase!.storage
        .from('qualification-objects')
        .remove(filePaths);

      if (deleteError) {
        console.error('Ошибка удаления файлов из Storage:', deleteError);
        throw new Error(`Ошибка удаления файлов: ${deleteError.message}`);
      }

      console.log(`Удалены файлы из Storage: ${filePaths.join(', ')}`);
    }
  } catch (error) {
    console.error('Ошибка при удалении файла снятия логгеров:', error);
    throw error;
  }
}
```

### **2. Обновлена функция `removeLoggerRemovalFile` в `src/components/QualificationWorkSchedule.tsx`:**

**До (только локальное удаление):**
```typescript
// Удаление файла снятия логгеров
const removeLoggerRemovalFile = (levelId: string) => {
  setLoggerRemovalFiles(prev => ({
    ...prev,
    [levelId]: null
  }));
  
  // Также очищаем файл из Storage (если он есть)
  setStorageFiles(prev => {
    const newStorageFiles = { ...prev };
    delete newStorageFiles[levelId];
    return newStorageFiles;
  });
};
```

**После (полное удаление):**
```typescript
// Удаление файла снятия логгеров
const removeLoggerRemovalFile = async (levelId: string) => {
  try {
    // Парсим ключ для получения номера зоны и уровня
    const match = levelId.match(/zone-(\d+)-level-(\d+)/);
    if (!match) {
      setError('Не удалось определить зону и уровень для удаления файла');
      return;
    }

    const zoneNumber = parseInt(match[1]);
    const measurementLevel = parseInt(match[2]);

    console.log('Удаляем файл логгера:', { levelId, zoneNumber, measurementLevel });

    // Получаем projectId для удаления данных из БД
    let currentProjectId = projectId;
    if (!currentProjectId) {
      const urlParams = new URLSearchParams(window.location.search);
      currentProjectId = urlParams.get('projectId') || undefined;
    }

    if (!currentProjectId) {
      setError('ProjectId не найден для удаления данных логгера');
      return;
    }

    // Удаляем файл из Supabase Storage
    await qualificationObjectService.deleteLoggerRemovalFile(qualificationObjectId, zoneNumber, measurementLevel);
    console.log('Файл удален из Storage');

    // Удаляем данные из базы данных
    const storageFile = storageFiles[levelId];
    if (storageFile && loggerDataService.isAvailable()) {
      const deleteResult = await loggerDataService.deleteLoggerData(
        currentProjectId,
        qualificationObjectId,
        storageFile.name
      );
      
      if (!deleteResult.success) {
        console.error('Ошибка удаления данных из БД:', deleteResult.error);
        setError(`Ошибка удаления данных из БД: ${deleteResult.error}`);
        return;
      }
      console.log('Данные удалены из БД');
    }

    // Обновляем локальное состояние
    setLoggerRemovalFiles(prev => ({
      ...prev,
      [levelId]: null
    }));
    
    setStorageFiles(prev => {
      const newStorageFiles = { ...prev };
      delete newStorageFiles[levelId];
      return newStorageFiles;
    });

    // Очищаем статусы парсинга
    setParsingStatus(prev => {
      const newStatus = { ...prev };
      delete newStatus[levelId];
      return newStatus;
    });

    setParsingResults(prev => {
      const newResults = { ...prev };
      delete newResults[levelId];
      return newResults;
    });

    setSuccess('Файл логгера успешно удален');
    setTimeout(() => setSuccess(null), 3000);

  } catch (error) {
    console.error('Ошибка удаления файла логгера:', error);
    setError(`Ошибка удаления файла: ${error instanceof Error ? error.message : 'Неизвестная ошибка'}`);
  }
};
```

### **3. Обновлен вызов функции в JSX:**

**До:**
```typescript
<button
  onClick={() => removeLoggerRemovalFile(fileKey)}
  className="inline-flex items-center px-2 py-1 border border-transparent text-xs font-medium rounded text-red-700 bg-red-100 hover:bg-red-200"
>
```

**После:**
```typescript
<button
  onClick={async () => await removeLoggerRemovalFile(fileKey)}
  className="inline-flex items-center px-2 py-1 border border-transparent text-xs font-medium rounded text-red-700 bg-red-100 hover:bg-red-200"
>
```

## 🔄 **Новый процесс удаления**

### **Полный цикл удаления файла логгера:**

1. **🔍 Парсинг параметров** - извлечение номера зоны и уровня из `levelId`
2. **📁 Удаление из Storage** - удаление файла из Supabase Storage
3. **💾 Удаление из БД** - удаление данных из таблиц `logger_data_records` и `logger_data_summary`
4. **🔄 Обновление UI** - очистка локального состояния
5. **✅ Уведомление** - показ сообщения об успешном удалении

### **Обработка ошибок:**
- ✅ Проверка корректности `levelId`
- ✅ Проверка наличия `projectId`
- ✅ Обработка ошибок удаления из Storage
- ✅ Обработка ошибок удаления из БД
- ✅ Информативные сообщения об ошибках

## 🎨 **Результат изменений**

### **До исправления:**
- ❌ Файлы удалялись только из UI
- ❌ Файлы оставались в Storage
- ❌ Данные оставались в БД
- ❌ После перезагрузки файлы снова появлялись

### **После исправления:**
- ✅ Файлы удаляются из Storage
- ✅ Данные удаляются из БД
- ✅ UI обновляется мгновенно
- ✅ Файлы не появляются после перезагрузки
- ✅ Показывается уведомление об успешном удалении

## 📊 **Структура удаления**

### **Удаление из Storage:**
```
qualification-objects/
└── logger-removal/
    └── {objectId}/
        └── zone-{zoneNumber}-level-{level}/
            └── {fileName}.vi2 ← Удаляется
```

### **Удаление из БД:**
```sql
-- Удаление детальных записей
DELETE FROM logger_data_records 
WHERE project_id = ? 
  AND qualification_object_id = ? 
  AND file_name = ?;

-- Удаление сводной информации
DELETE FROM logger_data_summary 
WHERE project_id = ? 
  AND qualification_object_id = ? 
  AND file_name = ?;
```

## 🧪 **Тестирование**

### **Для проверки исправления:**

1. **Загрузите файлы логгеров:**
   - Перейдите в "План-график проведения квалификационных работ"
   - Загрузите несколько `.vi2` файлов

2. **Удалите файл:**
   - Нажмите кнопку "Удалить" для любого файла
   - Проверьте, что файл исчез из таблицы

3. **Проверьте полное удаление:**
   - Перезагрузите страницу
   - Убедитесь, что файл не появился снова

4. **Проверьте уведомления:**
   - ✅ Должно появиться сообщение "Файл логгера успешно удален"
   - ❌ Не должно быть сообщений об ошибках

### **Ожидаемые логи:**
```
Удаляем файл логгера: {levelId: "zone-1-level-0", zoneNumber: 1, measurementLevel: 0}
Файл удален из Storage
Данные удалены из БД
```

## 📋 **Связанные файлы**

- `src/utils/qualificationObjectService.ts` - новый метод `deleteLoggerRemovalFile`
- `src/components/QualificationWorkSchedule.tsx` - обновленная функция `removeLoggerRemovalFile`
- `src/utils/loggerDataService.ts` - существующий метод `deleteLoggerData`

## 🎯 **Преимущества исправления**

1. **🔄 Корректная работа:**
   - Файлы действительно удаляются
   - UI обновляется мгновенно
   - Нет необходимости в перезагрузке

2. **💾 Целостность данных:**
   - Синхронизация между Storage и БД
   - Полное удаление всех связанных данных
   - Отсутствие "мертвых" записей

3. **🎨 Улучшенный UX:**
   - Мгновенная обратная связь
   - Информативные уведомления
   - Обработка ошибок

4. **🔧 Надежность:**
   - Проверка всех условий
   - Обработка исключений
   - Логирование операций

## 🎯 **Заключение**

Исправление полностью решает проблему с кнопкой "Удалить" в таблице "Файлы данных логгеров". Теперь файлы корректно удаляются из всех источников (Storage, БД, UI), и пользователи получают мгновенную обратную связь без необходимости перезагрузки страницы.

**Результат: полностью функциональная кнопка удаления с корректным обновлением UI!** 🎉



















