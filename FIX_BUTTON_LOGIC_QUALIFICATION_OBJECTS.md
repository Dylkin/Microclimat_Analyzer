# Исправление логики кнопок в колонке "Действия"

## 🎯 **Проблема**

При статусе договора "Согласовано" блокировка работала, но кнопки в колонке "Действия" были неактивны для всех объектов квалификации.

### **Ожидаемое поведение:**
- **Для выбранных объектов**: активны обе кнопки ("Просмотр" и "Выполнить")
- **Для невыбранных объектов**: активна только кнопка "Просмотр"

### **Фактическое поведение:**
- **Для всех объектов**: обе кнопки были неактивны

## 🔍 **Анализ проблемы**

### **Предыдущая логика (неправильная):**
```typescript
// Кнопка "Просмотр"
disabled={isCheckboxesBlocked}

// Кнопка "Выполнить" 
disabled={isCheckboxesBlocked}
```

### **Проблема:**
- Когда `isCheckboxesBlocked = true`, обе кнопки блокировались для всех объектов
- Не учитывался статус выбора объекта (`selectedObjects.has(obj.id)`)

## ✅ **Решение**

### **1. Кнопка "Просмотр" - всегда активна:**
```typescript
<button
  onClick={() => {
    setViewingObject(obj);
    setObjectMode('view');
  }}
  disabled={false} // Кнопка "Просмотр" всегда активна
  className="text-blue-600 hover:text-blue-900"
  title="Просмотреть объект квалификации"
>
  <Eye className="w-4 h-4" />
</button>
```

### **2. Кнопка "Выполнить" - только для выбранных объектов:**
```typescript
<button
  onClick={() => {
    setEditingObject(obj);
    setObjectMode('edit');
  }}
  disabled={!selectedObjects.has(obj.id)} // Активна только для выбранных объектов
  className={`${
    !selectedObjects.has(obj.id)
      ? 'text-gray-400 cursor-not-allowed opacity-50'
      : 'text-indigo-600 hover:text-indigo-900'
  }`}
  title={
    !selectedObjects.has(obj.id)
      ? 'Выполнение доступно только для выбранных объектов'
      : 'Выполнить'
  }
>
  <Play className="w-4 h-4" />
</button>
```

## 📊 **Результат**

### **Для выбранных объектов (например, "Помещение"):**
- ✅ **Кнопка "Просмотр"**: активна (синяя, кликабельная)
- ✅ **Кнопка "Выполнить"**: активна (синяя, кликабельная)

### **Для невыбранных объектов (например, "Холодильник", "Морозильник"):**
- ✅ **Кнопка "Просмотр"**: активна (синяя, кликабельная)
- ❌ **Кнопка "Выполнить"**: неактивна (серая, заблокированная)

## 🧪 **Тестирование**

### **Сценарий 1: Выбранный объект**
1. Откройте страницу "Согласование договора"
2. Убедитесь, что договор имеет статус "Согласовано"
3. В таблице "Объекты квалификации" найдите объект со статусом "Выбран"
4. **Ожидаемый результат**: обе кнопки активны

### **Сценарий 2: Невыбранный объект**
1. В той же таблице найдите объект со статусом "Не выбран"
2. **Ожидаемый результат**: только кнопка "Просмотр" активна

### **Сценарий 3: Блокировка выбора**
1. Убедитесь, что чекбоксы заменены на статичные индикаторы
2. Убедитесь, что предупреждение о блокировке отображается
3. **Ожидаемый результат**: выбор объектов заблокирован, но кнопки работают по новой логике

## 🎯 **Ключевые изменения**

1. **Кнопка "Просмотр"**: убрана зависимость от `isCheckboxesBlocked`
2. **Кнопка "Выполнить"**: изменена логика с `isCheckboxesBlocked` на `!selectedObjects.has(obj.id)`
3. **Стилизация**: обновлены классы и подсказки для корректного отображения состояний

**Теперь кнопки работают корректно в зависимости от статуса выбора объекта!** ✅




