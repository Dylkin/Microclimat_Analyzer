# Тестирование системы аудита

## 🚀 Быстрый старт

### 1. Создание таблицы аудита

Выполните в Supabase SQL Editor:

```sql
-- Скопируйте и выполните содержимое файла create_audit_logs_manual.sql
```

### 2. Запуск приложения

```bash
npm run dev
```

### 3. Тестирование функционала

#### Шаг 1: Вход в систему
1. Войдите как администратор
2. Перейдите в меню "Логи аудита"
3. Убедитесь, что страница загружается без ошибок

#### Шаг 2: Тестирование согласования документов
1. Перейдите в "Проекты квалификации"
2. Выберите проект со статусом "Согласование договора"
3. Нажмите "Выполнить"
4. В блоке "Согласование документов":
   - Нажмите "Согласовано" для любого документа
   - Нажмите "Отменить согласование"
5. Вернитесь в "Логи аудита"
6. Проверьте, что события записались:
   - `document_approved`
   - `document_approval_cancelled`

#### Шаг 3: Проверка фильтрации
1. В логах аудита используйте фильтры:
   - По действию: "Документ согласован"
   - По типу сущности: "Документ"
   - По дате: выберите сегодняшний день
2. Убедитесь, что фильтрация работает

#### Шаг 4: Экспорт данных
1. Нажмите "Экспорт CSV"
2. Проверьте, что файл скачался
3. Откройте файл в Excel/Google Sheets
4. Убедитесь, что данные корректны

## 🔍 Проверка в базе данных

### Просмотр записей аудита

```sql
SELECT 
    timestamp,
    user_name,
    user_role,
    action,
    entity_type,
    entity_name,
    details
FROM public.audit_logs 
ORDER BY timestamp DESC 
LIMIT 10;
```

### Проверка структуры таблицы

```sql
SELECT 
    column_name,
    data_type,
    is_nullable
FROM information_schema.columns 
WHERE table_schema = 'public' 
AND table_name = 'audit_logs'
ORDER BY ordinal_position;
```

### Проверка индексов

```sql
SELECT 
    indexname,
    indexdef
FROM pg_indexes 
WHERE tablename = 'audit_logs';
```

## ✅ Ожидаемые результаты

### При согласовании документа
- Запись в `audit_logs` с `action = 'document_approved'`
- `entity_type = 'document'`
- `details` содержит информацию о документе
- `user_name` и `user_role` заполнены

### При отмене согласования
- Запись в `audit_logs` с `action = 'document_approval_cancelled'`
- `details` содержит `fallback: true` если была ошибка

### В интерфейсе логов
- Таблица загружается без ошибок
- Фильтры работают корректно
- Статистика отображается правильно
- Экспорт CSV работает

## 🐛 Возможные проблемы

### Ошибка "Доступ запрещен"
- Убедитесь, что пользователь имеет роль `admin` или `administrator`
- Проверьте RLS политики в базе данных

### Ошибка "Supabase не настроен"
- Проверьте переменные окружения `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
- Убедитесь, что Supabase клиент инициализирован

### Логи не записываются
- Проверьте консоль браузера на ошибки
- Убедитесь, что таблица `audit_logs` создана
- Проверьте RLS политики для INSERT

### Ошибки линтера
- Все ошибки доступности исправлены
- Компоненты соответствуют стандартам

## 📊 Мониторинг

### Консоль браузера
Следите за сообщениями:
- `"Ошибка записи в аудит:"` - проблемы с записью
- `"Ошибка загрузки логов аудита:"` - проблемы с чтением

### База данных
Проверяйте:
- Количество записей в `audit_logs`
- Время последних записей
- Корректность данных

## 🎯 Критерии успеха

✅ **Таблица аудита создана** без ошибок  
✅ **События записываются** при согласовании документов  
✅ **Интерфейс логов** загружается и работает  
✅ **Фильтрация** функционирует корректно  
✅ **Экспорт CSV** работает  
✅ **Нет ошибок** в консоли браузера  
✅ **RLS политики** настроены правильно  

## 🔧 Дополнительные тесты

### Тест производительности
- Создайте 100+ записей аудита
- Проверьте скорость загрузки логов
- Убедитесь, что индексы работают

### Тест безопасности
- Попробуйте получить доступ к логам под обычным пользователем
- Убедитесь, что доступ запрещен
- Проверьте, что записи создаются только аутентифицированными пользователями

### Тест отказоустойчивости
- Отключите интернет при согласовании документа
- Убедитесь, что основная функциональность не нарушена
- Проверьте, что аудит записывается при восстановлении соединения

Система аудита готова к использованию! 🎉



















