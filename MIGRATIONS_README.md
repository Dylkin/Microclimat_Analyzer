# 📋 Руководство по применению миграций базы данных

## Обзор

Проект использует PostgreSQL в качестве основной базы данных. Миграции применяются через скрипт `server/scripts/apply-migrations.ts`.

## Структура миграций

- **`database_setup.sql`** - основной файл для создания структуры БД (таблицы, индексы, триггеры)
- **`supabase/migrations/`** - папка с дополнительными миграциями (применяются после основного файла)

## Применение миграций

### Автоматический способ (рекомендуется)

```bash
# Убедитесь, что настроены переменные окружения в .env
npm run migrate
```

Или напрямую через tsx:

```bash
tsx server/scripts/apply-migrations.ts
```

### Ручной способ

```bash
# Применение основного файла
psql -U microclimat_user -d microclimat -f database_setup.sql

# Применение миграций из папки (по порядку)
for file in supabase/migrations/*.sql; do
  psql -U microclimat_user -d microclimat -f "$file"
done
```

## Переменные окружения

Создайте файл `.env` в корне проекта:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=microclimat
DB_USER=postgres
DB_PASSWORD=your_password
```

## Отслеживание миграций

Скрипт автоматически создает таблицу `schema_migrations` для отслеживания примененных миграций. Это предотвращает повторное применение уже выполненных миграций.

## Проверка статуса

Для проверки примененных миграций:

```sql
SELECT filename, applied_at FROM schema_migrations ORDER BY applied_at;
```

## Создание новой миграции

1. Создайте файл в папке `supabase/migrations/` с именем в формате:
   ```
   YYYYMMDDHHMMSS_description.sql
   ```

2. Добавьте SQL-код миграции

3. Примените миграцию через скрипт

## Откат миграций

⚠️ **Внимание**: Текущий скрипт не поддерживает автоматический откат. Для отката миграций создайте обратную миграцию вручную.

## Устранение проблем

### Ошибка подключения к БД
- Проверьте переменные окружения в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя БД

### Ошибка применения миграции
- Проверьте синтаксис SQL в файле миграции
- Убедитесь, что предыдущие миграции применены
- Проверьте логи PostgreSQL

### Дублирование миграций
- Скрипт автоматически пропускает уже примененные миграции
- Если нужно применить заново, удалите запись из `schema_migrations`


