# Механизм сброса пароля пользователя

## 🎯 **Реализованная функциональность**

### ✅ **Полный цикл сброса пароля через email:**

1. **📧 Запрос сброса пароля** - пользователь вводит email на странице входа
2. **📨 Отправка письма** - Supabase отправляет письмо с ссылкой для сброса
3. **🔗 Переход по ссылке** - пользователь переходит по ссылке из письма
4. **🔒 Установка нового пароля** - пользователь вводит новый пароль с валидацией
5. **✅ Подтверждение** - пароль успешно обновлен

## 🔧 **Техническая реализация**

### **1. Обновлен `userService.ts`:**

**Добавлены новые методы:**
```typescript
// Отправка письма для сброса пароля
async sendPasswordResetEmail(email: string): Promise<void> {
  const { error } = await this.supabase.auth.resetPasswordForEmail(email, {
    redirectTo: `${window.location.origin}/reset-password`,
  });
}

// Обновление пароля через токен сброса
async updatePasswordWithToken(newPassword: string): Promise<void> {
  const { error } = await this.supabase.auth.updateUser({
    password: newPassword
  });
}
```

### **2. Обновлен `AuthContext.tsx`:**

**Добавлены функции в интерфейс:**
```typescript
interface AuthContextType {
  // ... существующие функции
  sendPasswordResetEmail: (email: string) => Promise<void>;
  updatePasswordWithToken: (newPassword: string) => Promise<void>;
}
```

**Реализованы функции:**
```typescript
const sendPasswordResetEmail = async (email: string): Promise<void> => {
  await userService.sendPasswordResetEmail(email);
};

const updatePasswordWithToken = async (newPassword: string): Promise<void> => {
  await userService.updatePasswordWithToken(newPassword);
};
```

### **3. Обновлен компонент `Login.tsx`:**

**Добавлены состояния:**
```typescript
const [showForgotPassword, setShowForgotPassword] = useState(false);
const [resetEmail, setResetEmail] = useState('');
const [resetLoading, setResetLoading] = useState(false);
const [resetMessage, setResetMessage] = useState('');
```

**Добавлена функция отправки письма:**
```typescript
const handleForgotPassword = async (e: React.FormEvent) => {
  await sendPasswordResetEmail(resetEmail);
  setResetMessage('Письмо для сброса пароля отправлено...');
};
```

**Обновлен UI:**
- ✅ Кнопка "Забыли пароль?" на форме входа
- ✅ Переключение между формами входа и сброса пароля
- ✅ Форма для ввода email для сброса
- ✅ Сообщения об успехе/ошибке
- ✅ Кнопка "Вернуться к входу"

### **4. Создан компонент `ResetPassword.tsx`:**

**Функциональность:**
- ✅ Проверка токенов в URL
- ✅ Валидация пароля (8+ символов, заглавные/строчные буквы, цифры)
- ✅ Подтверждение пароля
- ✅ Показ/скрытие пароля
- ✅ Визуальные индикаторы требований к паролю
- ✅ Обработка ошибок
- ✅ Страница успеха

**Валидация пароля:**
```typescript
const validatePassword = (password: string): string[] => {
  const errors: string[] = [];
  
  if (password.length < 8) {
    errors.push('Пароль должен содержать минимум 8 символов');
  }
  
  if (!/[A-Z]/.test(password)) {
    errors.push('Пароль должен содержать хотя бы одну заглавную букву');
  }
  
  if (!/[a-z]/.test(password)) {
    errors.push('Пароль должен содержать хотя бы одну строчную букву');
  }
  
  if (!/\d/.test(password)) {
    errors.push('Пароль должен содержать хотя бы одну цифру');
  }
  
  return errors;
};
```

### **5. Обновлен `App.tsx`:**

**Добавлена поддержка маршрута:**
```typescript
// Lazy import
const ResetPassword = lazy(() => import('./components/ResetPassword'));

// Проверка маршрута
useEffect(() => {
  const path = window.location.pathname;
  if (path === '/reset-password') {
    setCurrentPage('reset-password');
  }
}, []);

// Условный рендеринг
if (currentPage === 'reset-password') {
  return (
    <Suspense fallback={<LoadingSpinner />}>
      <ResetPassword />
    </Suspense>
  );
}
```

## 🎨 **Пользовательский интерфейс**

### **Страница входа:**

1. **Форма входа** (по умолчанию):
   - Поля email и пароль
   - Кнопка "Войти"
   - Кнопка "Забыли пароль?" внизу

2. **Форма сброса пароля** (при нажатии "Забыли пароль?"):
   - Поле email
   - Кнопка "Отправить письмо"
   - Кнопка "Вернуться к входу"
   - Сообщения об успехе/ошибке

### **Страница сброса пароля (`/reset-password`):**

1. **Форма установки пароля:**
   - Поле "Новый пароль" с показом/скрытием
   - Поле "Подтвердите пароль" с показом/скрытием
   - Блок требований к паролю с визуальными индикаторами
   - Кнопка "Обновить пароль"

2. **Страница успеха:**
   - Иконка успеха
   - Сообщение об успешном обновлении
   - Кнопка "Перейти к входу"

## 🔒 **Безопасность**

### **Валидация пароля:**
- ✅ Минимум 8 символов
- ✅ Хотя бы одна заглавная буква
- ✅ Хотя бы одна строчная буква
- ✅ Хотя бы одна цифра
- ✅ Визуальная обратная связь

### **Обработка токенов:**
- ✅ Проверка наличия токенов в URL
- ✅ Использование Supabase Auth для безопасного обновления
- ✅ Автоматическое истечение токенов

### **Обработка ошибок:**
- ✅ Валидация email адреса
- ✅ Проверка совпадения паролей
- ✅ Обработка недействительных/устаревших ссылок
- ✅ Информативные сообщения об ошибках

## 🚀 **Использование**

### **Для пользователя:**

1. **Запрос сброса пароля:**
   - На странице входа нажмите "Забыли пароль?"
   - Введите email адрес
   - Нажмите "Отправить письмо"
   - Проверьте почту

2. **Установка нового пароля:**
   - Перейдите по ссылке из письма
   - Введите новый пароль (соблюдая требования)
   - Подтвердите пароль
   - Нажмите "Обновить пароль"
   - Перейдите к входу с новым паролем

### **Для разработчика:**

**Отправка письма:**
```typescript
const { sendPasswordResetEmail } = useAuth();
await sendPasswordResetEmail('user@example.com');
```

**Обновление пароля:**
```typescript
const { updatePasswordWithToken } = useAuth();
await updatePasswordWithToken('newPassword123');
```

## 📧 **Настройка email**

### **Supabase настройки:**

1. **В панели Supabase:**
   - Перейдите в Authentication → Settings
   - Настройте SMTP для отправки писем
   - Или используйте встроенный email провайдер

2. **Шаблон письма:**
   - Supabase автоматически генерирует письмо
   - Ссылка ведет на `/reset-password` с токенами
   - Можно настроить кастомный шаблон

## 🔧 **Конфигурация**

### **Переменные окружения:**
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### **URL для сброса пароля:**
- По умолчанию: `${window.location.origin}/reset-password`
- Можно изменить в `userService.ts`

## ✅ **Результат**

После реализации:

- ✅ **Пользователи могут сбросить пароль** через email
- ✅ **Безопасная валидация** паролей
- ✅ **Удобный интерфейс** с переключением форм
- ✅ **Обработка ошибок** и информативные сообщения
- ✅ **Интеграция с Supabase Auth** для безопасности
- ✅ **Автоматическая маршрутизация** на страницу сброса
- ✅ **Визуальная обратная связь** для требований к паролю

**Механизм сброса пароля полностью реализован и готов к использованию!** 🎉

## 📁 **Измененные файлы**

- `src/utils/userService.ts` - добавлены методы сброса пароля
- `src/contexts/AuthContext.tsx` - добавлены функции в контекст
- `src/components/Login.tsx` - добавлена форма сброса пароля
- `src/components/ResetPassword.tsx` - новый компонент для установки пароля
- `src/App.tsx` - добавлена поддержка маршрута `/reset-password`

## 🎯 **Следующие шаги**

1. **Настройте SMTP** в Supabase для отправки писем
2. **Протестируйте** полный цикл сброса пароля
3. **Настройте кастомный шаблон** письма (опционально)
4. **Добавьте логирование** для аудита сбросов паролей



















