# Обновление логики чекбоксов на странице согласования договора

## 🎯 **Задача**

На странице "Согласование договора" после присвоения статуса "Согласовано" для договора в таблице "Объекты квалификации":

1. **Колонку "Выбрать все"** и все чекбоксы переводить в режим просмотра
2. **Для строк с неактивным чекбоксом** в колонке "Действия" иконку "Выполнить" делать неактивной

## 🔧 **Выполненные изменения**

### **1. Обновление логики отображения строк таблицы**

#### Было:
```typescript
{(isCheckboxesBlocked 
  ? objects.filter(obj => selectedObjects.has(obj.id))
  : objects
).map((obj) => (
```

#### Стало:
```typescript
{objects.map((obj) => (
```

**Изменение:** Теперь при `isCheckboxesBlocked = true` отображаются все объекты, а не только выбранные.

### **2. Обновление логики чекбоксов**

#### Было:
```typescript
<button
  onClick={() => handleObjectSelect(obj.id)}
  disabled={isCheckboxesBlocked}
  className={`flex items-center space-x-2 ${
    isCheckboxesBlocked 
      ? 'cursor-not-allowed opacity-50' 
      : 'hover:text-gray-700'
  }`}
  title={isCheckboxesBlocked ? 'Выбор объектов заблокирован после согласования договора' : ''}
>
  {selectedObjects.has(obj.id) ? (
    <CheckSquare className={`w-4 h-4 ${isCheckboxesBlocked ? 'text-gray-400' : 'text-indigo-600'}`} />
  ) : (
    <Square className="w-4 h-4 text-gray-400" />
  )}
</button>
```

#### Стало:
```typescript
{isCheckboxesBlocked ? (
  <div className="flex items-center space-x-2">
    {selectedObjects.has(obj.id) ? (
      <CheckSquare className="w-4 h-4 text-green-600" />
    ) : (
      <Square className="w-4 h-4 text-gray-400" />
    )}
    <span className="text-sm text-gray-500">
      {selectedObjects.has(obj.id) ? 'Выбран' : 'Не выбран'}
    </span>
  </div>
) : (
  <button
    onClick={() => handleObjectSelect(obj.id)}
    className="flex items-center space-x-2 hover:text-gray-700"
    title="Выбрать объект"
  >
    {selectedObjects.has(obj.id) ? (
      <CheckSquare className="w-4 h-4 text-indigo-600" />
    ) : (
      <Square className="w-4 h-4 text-gray-400" />
    )}
  </button>
)}
```

**Изменения:**
- **При `isCheckboxesBlocked = true`**: Чекбоксы отображаются в режиме просмотра с текстом "Выбран"/"Не выбран"
- **При `isCheckboxesBlocked = false`**: Чекбоксы остаются интерактивными
- **Выбранные объекты**: Отображаются зеленой галочкой
- **Невыбранные объекты**: Отображаются серым квадратом

### **3. Обновление логики кнопки "Выполнить"**

#### Было:
```typescript
<button
  onClick={() => {
    setEditingObject(obj);
    setObjectMode('edit');
  }}
  className="text-indigo-600 hover:text-indigo-900"
  title="Выполнить"
>
  <Play className="w-4 h-4" />
</button>
```

#### Стало:
```typescript
<button
  onClick={() => {
    setEditingObject(obj);
    setObjectMode('edit');
  }}
  disabled={isCheckboxesBlocked && !selectedObjects.has(obj.id)}
  className={`${
    isCheckboxesBlocked && !selectedObjects.has(obj.id)
      ? 'text-gray-400 cursor-not-allowed opacity-50'
      : 'text-indigo-600 hover:text-indigo-900'
  }`}
  title={
    isCheckboxesBlocked && !selectedObjects.has(obj.id)
      ? 'Выполнение заблокировано для неактивных объектов'
      : 'Выполнить'
  }
>
  <Play className="w-4 h-4" />
</button>
```

**Изменения:**
- **Кнопка "Выполнить"** становится неактивной для объектов с неактивными чекбоксами
- **Визуальное состояние**: Серый цвет, курсор "not-allowed", пониженная прозрачность
- **Tooltip**: Информативное сообщение о блокировке

## 📋 **Логика работы**

### **До согласования договора (`isCheckboxesBlocked = false`):**

1. **Чекбоксы интерактивные** - можно выбирать/снимать выбор
2. **Кнопка "Выбрать все"** работает
3. **Кнопка "Выполнить"** активна для всех объектов
4. **Отображаются все объекты** в таблице

### **После согласования договора (`isCheckboxesBlocked = true`):**

1. **Чекбоксы в режиме просмотра** - показывают статус "Выбран"/"Не выбран"
2. **Кнопка "Выбрать все"** заблокирована
3. **Кнопка "Выполнить"** активна только для выбранных объектов
4. **Отображаются все объекты** в таблице
5. **Выбранные объекты** отмечены зеленой галочкой
6. **Невыбранные объекты** отмечены серым квадратом

## 🎨 **Визуальные изменения**

### **Чекбоксы в режиме просмотра:**
- **Выбранные**: Зеленая галочка + текст "Выбран"
- **Невыбранные**: Серый квадрат + текст "Не выбран"
- **Нет интерактивности**: Нельзя кликать

### **Кнопка "Выполнить":**
- **Активная**: Синий цвет, hover эффект
- **Неактивная**: Серый цвет, курсор "not-allowed", opacity 50%

### **Кнопка "Выбрать все":**
- **Заблокированная**: Серый цвет, курсор "not-allowed", opacity 50%
- **Tooltip**: "Выбор объектов заблокирован после согласования договора"

## 🔍 **Ожидаемое поведение**

### **Сценарий 1: До согласования договора**
1. Пользователь может выбирать объекты чекбоксами
2. Кнопка "Выбрать все" работает
3. Кнопка "Выполнить" активна для всех объектов
4. Все объекты отображаются в таблице

### **Сценарий 2: После согласования договора**
1. Чекбоксы показывают статус выбора (зеленые/серые)
2. Кнопка "Выбрать все" заблокирована
3. Кнопка "Выполнить" активна только для выбранных объектов
4. Все объекты отображаются в таблице
5. Пользователь может просматривать все объекты, но выполнять только выбранные

## 🧪 **Тестирование**

### **Шаг 1: Проверка до согласования**
1. Откройте страницу "Согласование договора"
2. Убедитесь, что чекбоксы интерактивные
3. Проверьте, что кнопка "Выбрать все" работает
4. Убедитесь, что кнопка "Выполнить" активна для всех объектов

### **Шаг 2: Согласование договора**
1. Согласуйте договор (установите статус "Согласовано")
2. Проверьте, что `isCheckboxesBlocked` стал `true`

### **Шаг 3: Проверка после согласования**
1. Убедитесь, что чекбоксы показывают статус "Выбран"/"Не выбран"
2. Проверьте, что кнопка "Выбрать все" заблокирована
3. Убедитесь, что кнопка "Выполнить" активна только для выбранных объектов
4. Проверьте, что все объекты отображаются в таблице

## 📝 **Заключение**

Реализованные изменения обеспечивают:

- **Четкое разделение** между режимами до и после согласования
- **Визуальную индикацию** статуса объектов квалификации
- **Блокировку нежелательных действий** после согласования
- **Сохранение функциональности просмотра** для всех объектов
- **Ограничение выполнения** только для выбранных объектов

Теперь пользователи могут видеть все объекты квалификации после согласования договора, но выполнять действия только с теми, которые были выбраны ранее.





