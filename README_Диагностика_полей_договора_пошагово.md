# Пошаговая диагностика проблемы с полями договора

## ✅ Шаг 1: Проверка структуры базы данных - ВЫПОЛНЕНО

Результат проверки показал, что поля существуют:
- `contract_number` (text, nullable)
- `contract_date` (date, nullable)

**Статус**: ✅ Структура БД корректна

## 🔍 Шаг 2: Проверка данных в базе

Выполните SQL скрипт `check_project_contract_data.sql` в Supabase SQL Editor:

```sql
-- Проверяем все проекты с данными договора
SELECT 
  id,
  name,
  contract_number,
  contract_date,
  status,
  created_at,
  updated_at
FROM public.projects
WHERE contract_number IS NOT NULL 
   OR contract_date IS NOT NULL
ORDER BY updated_at DESC;
```

**Ожидаемый результат**: Список проектов с заполненными полями договора.

## 🔍 Шаг 3: Проверка загрузки данных в приложении

### 3.1 Откройте консоль браузера (F12)

### 3.2 Перейдите на страницу "Проекты квалификации"
- Выберите любой проект
- Перейдите на страницу "Согласование договора"

### 3.3 Проверьте логи в консоли

**Ожидаемые логи:**

```javascript
// При загрузке проекта
ProjectService: Данные проекта из БД: {
  id: "uuid",
  name: "Название проекта",
  contract_number: null,  // или строка
  contract_date: null,    // или дата
  rawData: { ... }
}

ProjectService: Преобразованный объект Project: {
  id: "uuid",
  name: "Название проекта", 
  contractNumber: null,   // или строка
  contractDate: null      // или объект Date
}

// При обновлении ContractFields
ContractFields: Обновление данных проекта {
  projectId: "uuid",
  contractNumber: null,   // или строка
  contractDate: null,     // или объект Date
  project: { ... }
}
```

## 🚨 Возможные проблемы и решения

### Проблема 1: В БД нет данных
**Симптом**: В логах `contract_number: null, contract_date: null`
**Решение**: Заполните поля договора через SQL или интерфейс

### Проблема 2: Данные есть в БД, но не загружаются
**Симптом**: В БД есть данные, но в логах `null`
**Решение**: Проверить RLS политики или права доступа

### Проблема 3: Данные загружаются, но не отображаются
**Симптом**: В логах есть данные, но UI показывает "Не указан"
**Решение**: Проверить логику отображения в ContractFields

## 🧪 Шаг 4: Тестирование сохранения

### 4.1 Попробуйте отредактировать поля договора
1. Нажмите "Редактировать" в блоке "Поля договора"
2. Введите номер договора и дату
3. Нажмите "Сохранить"

### 4.2 Проверьте логи сохранения

**Ожидаемые логи:**

```javascript
ContractFields: Сохранение полей договора {
  projectId: "uuid",
  updateData: { contractNumber: "12345", contractDate: "2024-01-01" },
  originalContractNumber: null,
  originalContractDate: null,
  newContractNumber: "12345",
  newContractDate: "2024-01-01"
}

ProjectService: Обновление проекта {
  projectId: "uuid",
  updates: { contractNumber: "12345", contractDate: Date },
  updateData: { contract_number: "12345", contract_date: "2024-01-01" }
}

ProjectService: Результат обновления { data: [...], error: null }
```

## 🔧 Шаг 5: Ручное тестирование через SQL

Если интерфейс не работает, попробуйте обновить данные напрямую:

```sql
-- Обновите данные договора для конкретного проекта
UPDATE public.projects 
SET 
  contract_number = 'TEST-12345',
  contract_date = '2024-01-01'
WHERE id = 'your-project-uuid-here';

-- Проверьте результат
SELECT 
  id,
  name,
  contract_number,
  contract_date
FROM public.projects 
WHERE id = 'your-project-uuid-here';
```

## 📋 Чек-лист диагностики

- [ ] ✅ Структура БД проверена (поля существуют)
- [ ] 🔍 Данные в БД проверены
- [ ] 🔍 Логи загрузки проверены
- [ ] 🔍 Логи сохранения проверены
- [ ] 🔧 Ручное тестирование через SQL выполнено

## 📞 Следующие шаги

1. **Выполните SQL скрипт** `check_project_contract_data.sql`
2. **Проверьте логи** в консоли браузера
3. **Попробуйте сохранить** данные через интерфейс
4. **Сообщите результаты** - какие логи видите, есть ли ошибки

## 🎯 Ожидаемый результат

После выполнения всех шагов поля договора должны:
1. **Загружаться** из базы данных
2. **Отображаться** в интерфейсе
3. **Сохраняться** при редактировании
4. **Обновляться** в базе данных

Если какой-то шаг не работает, это поможет точно определить причину проблемы.



















