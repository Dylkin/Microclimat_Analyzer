# Диагностика ошибки загрузки логов аудита

## 🔍 Пошаговая диагностика

### Шаг 1: Проверка таблицы в базе данных

Выполните SQL скрипт `check_audit_logs_table.sql` в Supabase SQL Editor:

```sql
-- Проверка существования таблицы audit_logs
SELECT 
    table_name,
    table_schema
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name = 'audit_logs';
```

**Ожидаемый результат:** Должна вернуться строка с `table_name = 'audit_logs'`

### Шаг 2: Создание таблицы (если не существует)

Если таблица не существует, выполните скрипт `create_audit_logs_manual.sql`:

```sql
-- Создание таблицы для аудита действий пользователей
CREATE TABLE IF NOT EXISTS public.audit_logs (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID NOT NULL,
    user_name TEXT NOT NULL,
    user_role TEXT NOT NULL,
    action TEXT NOT NULL,
    entity_type TEXT NOT NULL,
    entity_id TEXT NOT NULL,
    entity_name TEXT,
    details JSONB,
    timestamp TIMESTAMPTZ DEFAULT NOW() NOT NULL,
    ip_address INET,
    user_agent TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);
```

### Шаг 3: Проверка RLS политик

```sql
-- Проверка RLS политик
SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd
FROM pg_policies 
WHERE schemaname = 'public' 
AND tablename = 'audit_logs';
```

### Шаг 4: Проверка в браузере

1. **Откройте страницу аудита** в браузере
2. **Откройте консоль разработчика** (F12)
3. **Найдите сообщения** с префиксом `AuditService:` и `AuditLogs:`
4. **Проверьте ошибки** в консоли

### Шаг 5: Тест подключения

Выполните в консоли браузера:

```javascript
// Тест подключения к Supabase
const { supabase } = await import('./src/utils/supabaseClient.js');
console.log('Supabase клиент:', supabase);

// Тест запроса к таблице
const { data, error } = await supabase.from('audit_logs').select('*').limit(1);
console.log('Результат запроса:', { data, error });
```

## 🐛 Возможные причины ошибок

### 1. Таблица не существует
**Симптомы:** `relation "public.audit_logs" does not exist`
**Решение:** Выполните скрипт создания таблицы

### 2. Проблемы с RLS
**Симптомы:** `new row violates row-level security policy`
**Решение:** Проверьте и исправьте RLS политики

### 3. Проблемы с правами доступа
**Симптомы:** `permission denied for table audit_logs`
**Решение:** Убедитесь, что пользователь имеет права администратора

### 4. Проблемы с подключением
**Симптомы:** `Failed to fetch` или сетевые ошибки
**Решение:** Проверьте переменные окружения Supabase

## 🔧 Исправления

### Если таблица не существует:

1. **Выполните миграцию:**
```sql
-- Скопируйте и выполните содержимое create_audit_logs_manual.sql
```

2. **Проверьте создание:**
```sql
SELECT COUNT(*) FROM public.audit_logs;
```

### Если проблемы с RLS:

1. **Временно отключите RLS для тестирования:**
```sql
ALTER TABLE public.audit_logs DISABLE ROW LEVEL SECURITY;
```

2. **Или создайте правильные политики:**
```sql
-- Политика для чтения (только администраторы)
CREATE POLICY "audit_logs_select_policy" ON public.audit_logs
    FOR SELECT
    USING (
        EXISTS (
            SELECT 1 FROM public.users 
            WHERE users.id = auth.uid() 
            AND users.role IN ('admin', 'administrator')
        )
    );

-- Политика для записи (все аутентифицированные пользователи)
CREATE POLICY "audit_logs_insert_policy" ON public.audit_logs
    FOR INSERT
    WITH CHECK (auth.uid() IS NOT NULL);
```

### Если проблемы с правами:

1. **Проверьте роль пользователя:**
```sql
SELECT id, email, role FROM public.users WHERE id = auth.uid();
```

2. **Обновите роль на администратора:**
```sql
UPDATE public.users SET role = 'admin' WHERE id = auth.uid();
```

## 📊 Логирование для диагностики

Добавлено расширенное логирование в:

1. **`auditService.ts`:**
   - Логи начала загрузки
   - Логи результатов запроса
   - Детальные ошибки

2. **`AuditLogs.tsx`:**
   - Логи фильтров
   - Логи полученных данных
   - Улучшенные сообщения об ошибках

## 🎯 Проверка работоспособности

После исправления проблем:

1. **Перезагрузите страницу** аудита
2. **Проверьте консоль** на наличие ошибок
3. **Убедитесь**, что данные загружаются
4. **Протестируйте фильтры** и экспорт

## 📝 Создание тестовых данных

Если таблица пустая, создайте тестовую запись:

```sql
INSERT INTO public.audit_logs (
    user_id,
    user_name,
    user_role,
    action,
    entity_type,
    entity_id,
    entity_name,
    details
) VALUES (
    auth.uid(),
    'Тестовый пользователь',
    'admin',
    'document_approved',
    'document',
    'test-doc-123',
    'Тестовый документ',
    '{"test": true}'::jsonb
);
```

## ✅ Ожидаемый результат

После исправления всех проблем:

- ✅ Страница аудита загружается без ошибок
- ✅ В консоли нет ошибок с префиксом `AuditService:`
- ✅ Данные отображаются в таблице
- ✅ Фильтры работают корректно
- ✅ Экспорт в CSV функционирует

Если проблемы сохраняются, проверьте логи в консоли браузера и выполните соответствующие исправления.



















