# Диагностика отображения этапов квалификации

## 🎯 **Проблема**

На странице "Редактировать объект квалификации" в блоке "План график проведения квалификационных работ" отображаются только 3 блока с испытаниями, а должны отображаться все 7 этапов.

## 📋 **Ожидаемые этапы**

В константе `QUALIFICATION_STAGES` определено **7 этапов**:

1. **Расстановка логгеров** - Установка и настройка логгеров для мониторинга температуры
2. **Испытание на соответствие критериям в пустом объеме** - Проверка соответствия температурных характеристик в пустом состоянии
3. **Испытание на соответствие критериям в загруженном объеме** - Проверка соответствия температурных характеристик при полной загрузке
4. **Испытание на восстановление температуры после открытия двери** - Проверка времени восстановления температурного режима после открытия
5. **Испытание на отключение электропитания** - Проверка поведения системы при отключении электропитания
6. **Испытание на включение электропитания** - Проверка запуска системы после включения электропитания
7. **Снятие логгеров** - Демонтаж и извлечение логгеров после завершения испытаний

## 🔧 **Добавленное логирование**

### **1. Логирование инициализации этапов**

**В функции `initializeStages()`:**
```typescript
const initializeStages = () => {
  const initializedStages = QUALIFICATION_STAGES.map((stage, index) => ({
    id: `temp-stage-${index + 1}`,
    ...stage,
    startDate: '',
    endDate: '',
    isCompleted: false
  }));
  console.log('QualificationWorkSchedule: Инициализация этапов:', {
    totalStages: QUALIFICATION_STAGES.length,
    initializedStages: initializedStages.length,
    stageNames: initializedStages.map(s => s.name)
  });
  setStages(initializedStages);
};
```

**Ожидаемый вывод:**
```
QualificationWorkSchedule: Инициализация этапов: {
  totalStages: 7,
  initializedStages: 7,
  stageNames: [
    "Расстановка логгеров",
    "Испытание на соответствие критериям в пустом объеме",
    "Испытание на соответствие критериям в загруженном объеме",
    "Испытание на восстановление температуры после открытия двери",
    "Испытание на отключение электропитания",
    "Испытание на включение электропитания",
    "Снятие логгеров"
  ]
}
```

### **2. Логирование загрузки этапов из БД**

**В функции `loadSchedule()`:**
```typescript
console.log('QualificationWorkSchedule: Загруженные этапы из БД:', {
  count: savedStages.length,
  stages: savedStages.map(s => ({ id: s.id, name: s.stageName }))
});
console.log('QualificationWorkSchedule: Преобразованные этапы:', {
  count: convertedStages.length,
  stages: convertedStages.map(s => ({ id: s.id, name: s.name }))
});
```

**Ожидаемый вывод при загрузке из БД:**
```
QualificationWorkSchedule: Загруженные этапы из БД: {
  count: 7,
  stages: [
    { id: "uuid-1", name: "Расстановка логгеров" },
    { id: "uuid-2", name: "Испытание на соответствие критериям в пустом объеме" },
    // ... остальные этапы
  ]
}
```

### **3. Логирование рендеринга этапов**

**В JSX рендеринга:**
```typescript
{(() => {
  console.log('QualificationWorkSchedule: Рендеринг этапов:', {
    stagesCount: stages.length,
    stageNames: stages.map(s => s.name)
  });
  return stages.map((stage, index) => (
    // ... рендеринг этапа
  ));
})()}
```

**Ожидаемый вывод:**
```
QualificationWorkSchedule: Рендеринг этапов: {
  stagesCount: 7,
  stageNames: [
    "Расстановка логгеров",
    "Испытание на соответствие критериям в пустом объеме",
    // ... все 7 этапов
  ]
}
```

## 🔍 **Возможные причины проблемы**

### **1. Проблема с загрузкой из БД**
- В базе данных сохранены только 3 этапа вместо 7
- Ошибка в запросе к БД
- Проблема с преобразованием данных

### **2. Проблема с инициализацией**
- Константа `QUALIFICATION_STAGES` содержит не все этапы
- Ошибка в функции `initializeStages()`
- Проблема с маппингом этапов

### **3. Проблема с рендерингом**
- Фильтрация этапов в JSX
- Ошибка в логике отображения
- Проблема с состоянием компонента

## 🧪 **Инструкции для диагностики**

### **Шаг 1: Проверьте консоль браузера**

1. Откройте страницу "Редактировать объект квалификации"
2. Откройте Developer Tools (F12)
3. Перейдите на вкладку Console
4. Найдите логи с префиксом `QualificationWorkSchedule:`

### **Шаг 2: Анализируйте логи**

**Проверьте логи инициализации:**
```
QualificationWorkSchedule: Инициализация этапов: {
  totalStages: 7,  // Должно быть 7
  initializedStages: 7,  // Должно быть 7
  stageNames: [...]  // Должно содержать все 7 названий
}
```

**Проверьте логи загрузки из БД:**
```
QualificationWorkSchedule: Загруженные этапы из БД: {
  count: 7,  // Должно быть 7, если данные есть в БД
  stages: [...]  // Должно содержать все 7 этапов
}
```

**Проверьте логи рендеринга:**
```
QualificationWorkSchedule: Рендеринг этапов: {
  stagesCount: 7,  // Должно быть 7
  stageNames: [...]  // Должно содержать все 7 названий
}
```

### **Шаг 3: Определите источник проблемы**

**Если `totalStages: 7`, но `stagesCount: 3`:**
- Проблема в загрузке из БД или преобразовании данных

**Если `totalStages: 3`:**
- Проблема в константе `QUALIFICATION_STAGES`

**Если все логи показывают 7 этапов, но отображается 3:**
- Проблема в CSS или рендеринге

## 🔧 **Возможные решения**

### **Решение 1: Если проблема в БД**
```sql
-- Проверьте количество этапов в БД
SELECT COUNT(*) FROM qualification_work_schedule 
WHERE qualification_object_id = 'your-object-id';

-- Проверьте названия этапов
SELECT stage_name FROM qualification_work_schedule 
WHERE qualification_object_id = 'your-object-id'
ORDER BY created_at;
```

### **Решение 2: Если проблема в инициализации**
- Проверьте константу `QUALIFICATION_STAGES`
- Убедитесь, что все 7 этапов определены
- Проверьте функцию `initializeStages()`

### **Решение 3: Если проблема в рендеринге**
- Проверьте CSS стили
- Убедитесь, что нет скрытых элементов
- Проверьте логику условного рендеринга

## 📊 **Ожидаемые результаты**

### **После исправления:**
- ✅ Отображаются все 7 этапов
- ✅ Логи показывают `stagesCount: 7`
- ✅ Все этапы доступны для редактирования
- ✅ Корректная нумерация этапов (1-7)

### **Текущее состояние:**
- ❌ Отображаются только 3 этапа
- ❌ Неполная информация в логах
- ❌ Пользователь не видит все этапы

## 📁 **Связанные файлы**

- `src/components/QualificationWorkSchedule.tsx` - основной компонент с логикой этапов
- `src/components/QualificationObjectForm.tsx` - форма объекта квалификации
- `src/utils/qualificationWorkScheduleService.ts` - сервис для работы с этапами

## 🎯 **Следующие шаги**

1. **Запустить проект** и проверить логи в консоли
2. **Проанализировать логи** для определения источника проблемы
3. **Применить соответствующее решение** на основе диагностики
4. **Проверить результат** - все 7 этапов должны отображаться

**Цель: обеспечить отображение всех 7 этапов квалификации в блоке "План график проведения квалификационных работ"!** 🎉



















