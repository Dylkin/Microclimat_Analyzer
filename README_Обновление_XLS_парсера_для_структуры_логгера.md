# Обновление XLS парсера для структуры данных логгера

## 🎯 **Задача**

Обновить XLS парсер для работы со специфической структурой файлов данных логгера, которая включает метаданные в верхней части и основную таблицу данных с колонками `id`, `Дата/время`, `Температура[°C]`.

## 📊 **Структура файла данных логгера**

### **1. Метаданные (строки 1-8):**
```
A1: Название прибора: DL-019
A2: Время запуска: 04.06.2025 9:00:00
A3: Время окончания: 15.06.2025 11:39:00
A4: Каналы измерения: 1
A5: Измеренные значения: 16000
A6: 58963022

D2: Минимум: 13,3
F3: Максимум: 24,3
H3: Среднее значение: 18,306
I3: Граничные значения: 2,0/25,0
F1: 05.10.2025 20:52:15
```

### **2. Основная таблица данных (начиная со строки 9):**
```
Строка 9 (заголовки):
A9: id
C9: Дата/время
E9: Температура[°C]

Строки 10+ (данные):
A10: 1, C10: 04.06.2025 9:00:00, E10: 20,70
A11: 2, C11: 04.06.2025 9:01:00, E11: 20,80
A12: 3, C12: 04.06.2025 9:02:00, E12: 20,90
...
```

### **3. Особенности структуры:**
- **Пустые колонки:** B и D разделяют основные колонки
- **Формат даты:** DD.MM.YYYY HH:MM:SS
- **Формат температуры:** Числа с запятой как разделителем десятичных знаков (русская локаль)
- **Метаданные:** Информация об устройстве в верхних строках

## 🔧 **Выполненные изменения**

### **1. Добавлена функция поиска структуры данных логгера**

**Новая функция `findLoggerDataStructure()`:**
```typescript
private findLoggerDataStructure(jsonData: any[][]): {
  headerRow: number;
  dataStartRow: number;
  idColumn: number;
  timestampColumn: number;
  temperatureColumn: number;
  deviceInfo?: {
    deviceName?: string;
    startTime?: string;
    endTime?: string;
    channels?: number;
    measurements?: number;
  };
} | null
```

**Логика поиска:**
- ✅ Ищет строку с заголовками "id", "Дата/время", "Температура[°C]"
- ✅ Определяет индексы колонок автоматически
- ✅ Извлекает метаданные устройства из верхних строк
- ✅ Возвращает структуру для дальнейшего парсинга

### **2. Добавлена функция извлечения метаданных устройства**

**Новая функция `extractDeviceInfo()`:**
```typescript
private extractDeviceInfo(jsonData: any[][], headerRow: number): any
```

**Извлекаемые метаданные:**
- ✅ **Название прибора** - из ячейки рядом с "Название прибора"
- ✅ **Время запуска** - из ячейки рядом с "Время запуска"
- ✅ **Время окончания** - из ячейки рядом с "Время окончания"
- ✅ **Каналы измерения** - из ячейки рядом с "Каналы измерения"
- ✅ **Измеренные значения** - из ячейки рядом с "Измеренные значения"

### **3. Добавлена специализированная функция парсинга данных логгера**

**Новая функция `parseLoggerData()`:**
```typescript
private parseLoggerData(jsonData: any[][], fileName: string, structure: any): ParsedFileData
```

**Функциональность:**
- ✅ Парсит данные начиная с определенной строки
- ✅ Использует найденные индексы колонок
- ✅ Создает метаданные устройства из извлеченной информации
- ✅ Валидирует и обрабатывает каждую строку данных

### **4. Добавлена функция парсинга строк данных логгера**

**Новая функция `parseLoggerRow()`:**
```typescript
private parseLoggerRow(row: any[], structure: any): MeasurementRecord | null
```

**Обработка данных:**
- ✅ Извлекает ID для валидации
- ✅ Парсит дату/время из колонки C
- ✅ Парсит температуру из колонки E
- ✅ Валидирует данные (диапазон температуры)
- ✅ Возвращает структурированную запись измерения

### **5. Обновлена функция парсинга чисел**

**Улучшенная функция `parseNumber()`:**
```typescript
private parseNumber(value: any): number | null
```

**Поддержка русской локали:**
- ✅ Замена запятой на точку для правильного парсинга
- ✅ Обработка чисел с запятой как разделителем десятичных знаков
- ✅ Валидация числовых значений

### **6. Добавлена функция стандартного парсинга**

**Новая функция `parseStandardData()`:**
```typescript
private parseStandardData(jsonData: any[][], fileName: string): ParsedFileData
```

**Назначение:**
- ✅ Обработка файлов без структуры логгера
- ✅ Обратная совместимость с обычными Excel файлами
- ✅ Стандартный поиск колонок по заголовкам

## 🔄 **Логика работы обновленного парсера**

### **1. Определение типа файла:**
```
Загрузка файла
    ↓
Поиск структуры данных логгера
    ↓
┌─────────────────┬─────────────────┐
│ Найдена         │ Не найдена      │
│ структура       │ структура       │
│ логгера         │ логгера         │
│        ↓        │        ↓        │
│ parseLoggerData │ parseStandardData│
└─────────────────┴─────────────────┘
    ↓
Сохранение в БД
```

### **2. Поиск структуры логгера:**
```
Сканирование строк файла
    ↓
Поиск заголовков: "id", "Дата/время", "Температура[°C]"
    ↓
┌─────────────────┬─────────────────┐
│ Заголовки       │ Заголовки       │
│ найдены         │ не найдены      │
│        ↓        │        ↓        │
│ Извлечение      │ Стандартный     │
│ метаданных      │ парсинг         │
│ устройства      │                 │
└─────────────────┴─────────────────┘
```

### **3. Парсинг данных логгера:**
```
Извлечение метаданных из верхних строк
    ↓
Определение индексов колонок
    ↓
Парсинг данных начиная с dataStartRow
    ↓
Валидация каждой строки
    ↓
Создание MeasurementRecord
    ↓
Формирование результата
```

## 📋 **Поддерживаемые форматы данных**

### **1. Структура данных логгера:**
- ✅ Метаданные в верхней части файла
- ✅ Заголовки: `id`, `Дата/время`, `Температура[°C]`
- ✅ Данные в колонках A, C, E
- ✅ Формат даты: DD.MM.YYYY HH:MM:SS
- ✅ Формат температуры: числа с запятой

### **2. Стандартная структура Excel:**
- ✅ Заголовки в первой строке
- ✅ Автоматический поиск колонок
- ✅ Поддержка различных форматов дат
- ✅ Поддержка влажности (опционально)

## 🧪 **Тестирование**

### **Для проверки функциональности:**

1. **Подготовьте файл с структурой логгера:**
   - Создайте Excel файл с метаданными в верхней части
   - Добавьте заголовки: `id`, `Дата/время`, `Температура[°C]`
   - Добавьте данные в колонки A, C, E
   - Используйте формат даты DD.MM.YYYY HH:MM:SS
   - Используйте запятую для десятичных знаков в температуре

2. **Загрузите файл в систему:**
   - Откройте страницу "Редактировать объект квалификации"
   - Перейдите к этапу "Снятие логгеров"
   - Загрузите подготовленный файл

3. **Проверьте логи парсинга:**
   ```
   XLSParser: Найдена структура данных логгера: {
     headerRow: 8,
     dataStartRow: 9,
     idColumn: 0,
     timestampColumn: 2,
     temperatureColumn: 4,
     deviceInfo: {
       deviceName: "DL-019",
       startTime: "04.06.2025 9:00:00",
       endTime: "15.06.2025 11:39:00",
       channels: 1,
       measurements: 16000
     }
   }
   ```

4. **Проверьте результат:**
   - ✅ Файл должен обработаться без ошибок
   - ✅ Метаданные устройства должны извлечься
   - ✅ Данные должны сохраниться в БД
   - ✅ Статус должен быть "Обработан"

### **Ожидаемое поведение:**

**При загрузке файла с структурой логгера:**
- ✅ Автоматическое определение структуры
- ✅ Извлечение метаданных устройства
- ✅ Парсинг данных с правильными индексами колонок
- ✅ Обработка русской локали (запятая в числах)
- ✅ Валидация данных

**При загрузке обычного Excel файла:**
- ✅ Стандартный парсинг по заголовкам
- ✅ Обратная совместимость
- ✅ Поддержка различных форматов

## 📁 **Измененные файлы**

- `src/utils/xlsParser.ts` - обновлен парсер для поддержки структуры логгера
- `src/components/QualificationWorkSchedule.tsx` - обновлена информация о поддерживаемых форматах

## 🎯 **Преимущества обновления**

### **1. 🎯 Специализированная обработка:**
- Точное распознавание структуры данных логгера
- Извлечение метаданных устройства
- Оптимизированный парсинг для конкретного формата

### **2. 🔧 Улучшенная совместимость:**
- Поддержка русской локали
- Обработка запятой как разделителя десятичных знаков
- Корректный парсинг дат в формате DD.MM.YYYY

### **3. 📊 Расширенные метаданные:**
- Извлечение информации об устройстве
- Сохранение времени запуска/окончания
- Информация о количестве каналов и измерений

### **4. 🔄 Обратная совместимость:**
- Поддержка стандартных Excel файлов
- Автоматическое определение типа структуры
- Fallback на стандартный парсинг

## 🔍 **Логирование и отладка**

### **Логи поиска структуры:**
```
XLSParser: Найдены заголовки в строке 9 {
  id: 0,
  timestamp: 2,
  temperature: 4
}
```

### **Логи извлечения метаданных:**
```
XLSParser: Извлеченные метаданные: {
  deviceName: "DL-019",
  startTime: "04.06.2025 9:00:00",
  endTime: "15.06.2025 11:39:00",
  channels: 1,
  measurements: 16000
}
```

### **Логи парсинга данных:**
```
XLSParser: Начинаем парсинг данных логгера с строки 10
XLSParser: Парсинг данных логгера завершен: {
  recordCount: 16000,
  startDate: "2025-06-04T09:00:00.000Z",
  endDate: "2025-06-15T11:39:00.000Z",
  deviceInfo: {...}
}
```

## 🎯 **Заключение**

XLS парсер обновлен для работы со структурой данных логгера:

- **Автоматическое определение:** Парсер автоматически распознает структуру логгера
- **Извлечение метаданных:** Информация об устройстве извлекается из верхних строк
- **Специализированный парсинг:** Оптимизированная обработка данных логгера
- **Поддержка локали:** Корректная обработка русской локали
- **Обратная совместимость:** Поддержка стандартных Excel файлов

**Результат: парсер теперь корректно обрабатывает файлы данных логгера с метаданными и извлекает всю необходимую информацию для анализа!** 🎉



















