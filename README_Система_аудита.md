# Система аудита действий пользователей

## 📋 Описание

Система аудита предназначена для отслеживания и записи всех важных действий пользователей в системе Microclimat Analyzer. Все события сохраняются в базе данных с подробной информацией о пользователе, времени, типе действия и дополнительных деталях.

## 🗄️ Структура базы данных

### Таблица `audit_logs`

```sql
CREATE TABLE public.audit_logs (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID NOT NULL,
    user_name TEXT NOT NULL,
    user_role TEXT NOT NULL,
    action TEXT NOT NULL,
    entity_type TEXT NOT NULL,
    entity_id TEXT NOT NULL,
    entity_name TEXT,
    details JSONB,
    timestamp TIMESTAMPTZ DEFAULT NOW() NOT NULL,
    ip_address INET,
    user_agent TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);
```

## 🎯 Отслеживаемые события

### Документы
- `document_approved` - Документ согласован
- `document_rejected` - Документ отклонен
- `document_approval_cancelled` - Согласование отменено
- `document_uploaded` - Документ загружен
- `document_deleted` - Документ удален

### Проекты
- `project_created` - Проект создан
- `project_updated` - Проект обновлен
- `project_status_changed` - Статус проекта изменен

### Пользователи
- `user_login` - Вход в систему
- `user_logout` - Выход из системы

### Договоры
- `contract_fields_updated` - Поля договора обновлены

### Объекты квалификации
- `qualification_object_created` - Объект квалификации создан
- `qualification_object_updated` - Объект квалификации обновлен
- `qualification_object_deleted` - Объект квалификации удален

### Данные логгеров
- `logger_data_uploaded` - Данные логгеров загружены
- `logger_data_parsed` - Данные логгеров обработаны

### Отчеты
- `report_generated` - Отчет сгенерирован
- `report_approved` - Отчет согласован
- `report_rejected` - Отчет отклонен

## 🚀 Установка

### 1. Создание таблицы аудита

Выполните SQL скрипт в Supabase SQL Editor:

```sql
-- Скопируйте содержимое файла create_audit_logs_manual.sql
```

### 2. Проверка создания таблицы

```sql
SELECT 
    table_name,
    column_name,
    data_type,
    is_nullable
FROM information_schema.columns 
WHERE table_schema = 'public' 
AND table_name = 'audit_logs'
ORDER BY ordinal_position;
```

## 🔧 Использование

### Просмотр логов аудита

1. **Войдите в систему** как администратор
2. **Перейдите в меню** "Логи аудита"
3. **Используйте фильтры** для поиска нужных событий:
   - По типу действия
   - По типу сущности
   - По дате
4. **Экспортируйте данные** в CSV формат при необходимости

### Программное использование

```typescript
import { auditService } from '../utils/auditService';

// Создание записи аудита
await auditService.createAuditLog({
  userId: 'user-id',
  userName: 'Иван Иванов',
  userRole: 'admin',
  action: 'document_approved',
  entityType: 'document',
  entityId: 'doc-123',
  entityName: 'Договор поставки',
  details: {
    documentType: 'contract',
    previousStatus: 'pending',
    newStatus: 'approved'
  }
});

// Получение логов с фильтрацией
const logs = await auditService.getAuditLogs({
  action: 'document_approved',
  startDate: new Date('2025-01-01'),
  endDate: new Date('2025-01-31'),
  limit: 100
});
```

## 🔒 Безопасность

### Row Level Security (RLS)

- **Чтение**: Только администраторы могут просматривать логи аудита
- **Запись**: Все аутентифицированные пользователи могут создавать записи
- **Изменение/Удаление**: Запрещено (логи неизменяемы)

### Политики доступа

```sql
-- Только администраторы могут читать
CREATE POLICY "audit_logs_select_policy" ON public.audit_logs
    FOR SELECT
    USING (
        EXISTS (
            SELECT 1 FROM public.users 
            WHERE users.id = auth.uid() 
            AND users.role IN ('admin', 'administrator')
        )
    );

-- Все аутентифицированные пользователи могут создавать
CREATE POLICY "audit_logs_insert_policy" ON public.audit_logs
    FOR INSERT
    WITH CHECK (auth.uid() IS NOT NULL);
```

## 📊 Интеграция с существующим функционалом

### Согласование документов

Аудит автоматически интегрирован в компонент `DocumentApprovalActions`:

- При нажатии "Согласовано" → записывается `document_approved`
- При нажатии "Отменить согласование" → записывается `document_approval_cancelled`
- При отклонении → записывается `document_rejected`

### Детали записи

Каждая запись содержит:
- **Основную информацию**: пользователь, время, действие
- **Контекст**: тип документа, предыдущий и новый статус
- **Техническую информацию**: IP адрес, User Agent
- **Дополнительные детали**: комментарии, метаданные

## 🔍 Мониторинг и анализ

### Статистика

На странице логов аудита отображается:
- Общее количество записей
- Количество согласований
- Количество отклонений
- Количество уникальных пользователей

### Экспорт данных

- **CSV формат** для анализа в Excel/Google Sheets
- **Фильтрация** перед экспортом
- **Автоматическое именование** файлов с датой

## 🛠️ Расширение системы

### Добавление новых событий

1. **Обновите типы** в `src/types/AuditLog.ts`:
```typescript
export type AuditAction = 
  | 'existing_action'
  | 'new_action'; // Добавьте новое действие
```

2. **Добавьте обработку** в соответствующий компонент:
```typescript
await auditService.createAuditLog({
  // ... параметры
  action: 'new_action',
  // ...
});
```

3. **Обновите интерфейс** в `AuditLogs.tsx`:
```typescript
const labels: Record<AuditAction, string> = {
  // ...
  new_action: 'Описание нового действия'
};
```

## 📝 Примеры использования

### Отслеживание изменений проекта

```typescript
// При обновлении проекта
await auditService.createAuditLog({
  userId: user.id,
  userName: user.fullName,
  userRole: user.role,
  action: 'project_updated',
  entityType: 'project',
  entityId: project.id,
  entityName: project.name,
  details: {
    changedFields: ['status', 'description'],
    oldValues: { status: 'draft', description: 'Old desc' },
    newValues: { status: 'active', description: 'New desc' }
  }
});
```

### Отслеживание загрузки файлов

```typescript
// При загрузке файла
await auditService.createAuditLog({
  userId: user.id,
  userName: user.fullName,
  userRole: user.role,
  action: 'logger_data_uploaded',
  entityType: 'logger_data',
  entityId: fileId,
  entityName: fileName,
  details: {
    fileSize: file.size,
    fileType: file.type,
    projectId: project.id
  }
});
```

## ⚠️ Важные замечания

1. **Производительность**: Индексы созданы для оптимизации запросов
2. **Хранение**: Логи хранятся неограниченно долго (настройте архивирование при необходимости)
3. **Приватность**: IP адреса и User Agent сохраняются для безопасности
4. **Надежность**: Ошибки аудита не прерывают основную функциональность

## 🔧 Техническая поддержка

При возникновении проблем:

1. **Проверьте права доступа** пользователя
2. **Убедитесь в корректности RLS политик**
3. **Проверьте логи консоли** на ошибки
4. **Убедитесь в доступности Supabase**

Система аудита обеспечивает полную прозрачность действий пользователей и помогает в анализе использования системы.



















