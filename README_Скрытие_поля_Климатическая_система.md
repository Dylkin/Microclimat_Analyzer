# Скрытие поля "Климатическая установка" для холодильника и морозильника

## Описание изменений

Для объектов квалификации типа "Холодильник" и "Морозильник" поле "Климатическая установка" больше не отображается, так как эти типы объектов не имеют климатических установок.

## Измененные файлы

### 1. `src/components/QualificationObjectForm.tsx`

**Изменение:** Добавлено условие для скрытия поля "Климатическая установка"

```tsx
{/* Климатическая установка - не отображается для холодильника и морозильника */}
{formData.type !== 'холодильник' && formData.type !== 'морозильник' && (
  <div>
    <label className="block text-sm font-medium text-gray-700 mb-1">
      Климатическая установка
    </label>
    <input
      type="text"
      value={formData.climateSystem}
      onChange={(e) => handleInputChange('climateSystem', e.target.value)}
      className={`w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 ${mode === 'view' ? 'bg-gray-100 cursor-not-allowed' : ''}`}
      readOnly={mode === 'view'}
      placeholder="Введите тип климатической системы"
    />
  </div>
)}
```

### 2. `src/components/QualificationObjectsTable.tsx`

**Изменение:** Добавлен комментарий, подтверждающий, что климатическая система не отображается для холодильника и морозильника

```tsx
case 'холодильник':
case 'морозильник':
  return (
    <div className="text-sm text-gray-600">
      {obj.serialNumber && <div>🔢 S/N: {obj.serialNumber}</div>}
      {obj.inventoryNumber && <div>📋 Инв. №: {obj.inventoryNumber}</div>}
      {obj.manufacturer && <div>🏭 {obj.manufacturer}</div>}
      {obj.measurementZones && obj.measurementZones.length > 0 && (
        <div>📍 Зон измерения: {obj.measurementZones.length}</div>
      )}
      {/* Климатическая установка не отображается для холодильника и морозильника */}
    </div>
  );
```

## Результат

Теперь при создании или редактировании объектов квалификации типа "Холодильник" и "Морозильник":

1. ✅ **Поле "Климатическая установка" не отображается** в форме создания/редактирования
2. ✅ **Поле "Климатическая установка" не отображается** в таблице объектов квалификации
3. ✅ **Для других типов объектов** (помещение, автомобиль, холодильная камера) поле продолжает отображаться как обычно

## Типы объектов, для которых поле отображается

- ✅ **Помещение** - отображается
- ✅ **Автомобиль** - отображается  
- ✅ **Холодильная камера** - отображается
- ❌ **Холодильник** - НЕ отображается
- ❌ **Морозильник** - НЕ отображается

## Тестирование

Для проверки изменений:

1. Создайте новый объект квалификации типа "Холодильник"
2. Убедитесь, что поле "Климатическая установка" не отображается в форме
3. Создайте новый объект квалификации типа "Морозильник"  
4. Убедитесь, что поле "Климатическая установка" не отображается в форме
5. Создайте объект другого типа (например, "Помещение")
6. Убедитесь, что поле "Климатическая установка" отображается как обычно











