# Автоматическое создание зоны измерения "Внешний датчик"

## 🎯 **Задача**

При создании этапа "Расстановка логгеров" в плане графика проведения квалификационных работ для объекта "VW Crafter" автоматически создавать зону измерения "Внешний датчик".

## ✅ **Реализованное решение**

### 1. Добавлена функция создания зоны "Внешний датчик"

**Файл:** `src/utils/qualificationWorkScheduleService.ts`

```typescript
/**
 * Создание зоны измерения "Внешний датчик" для объекта квалификации
 */
private async createExternalSensorZone(qualificationObjectId: string): Promise<void> {
  try {
    // Получаем текущие зоны измерения объекта
    const object = await qualificationObjectService.getQualificationObjectById(qualificationObjectId);
    const existingZones = object.measurementZones || [];
    
    // Проверяем, есть ли уже зона "Внешний датчик"
    const hasExternalZone = existingZones.some(zone => 
      zone.measurementLevels.some(level => 
        level.equipmentName && level.equipmentName.toLowerCase().includes('внешний')
      )
    );
    
    if (hasExternalZone) {
      console.log('Зона "Внешний датчик" уже существует');
      return;
    }
    
    // Создаем новую зону измерения "Внешний датчик"
    const externalZone = {
      id: `zone-external-${Date.now()}`,
      zoneNumber: existingZones.length + 1,
      measurementLevels: [
        {
          id: `level-external-${Date.now()}`,
          level: 1.0, // Высота 1 метр от пола
          equipmentId: `equipment-external-${Date.now()}`,
          equipmentName: 'Внешний датчик'
        }
      ]
    };
    
    // Добавляем новую зону к существующим
    const updatedZones = [...existingZones, externalZone];
    
    // Сохраняем обновленные зоны измерения
    await qualificationObjectService.updateMeasurementZones(qualificationObjectId, updatedZones);
    
    console.log('Зона "Внешний датчик" успешно создана');
  } catch (error) {
    console.error('Ошибка создания зоны "Внешний датчик":', error);
    // Не прерываем выполнение основного процесса
  }
}
```

### 2. Интеграция с созданием этапов

**Автоматическое создание при создании всех этапов:**

```typescript
async createAllStages(qualificationObjectId: string, projectId?: string): Promise<QualificationWorkStage[]> {
  // ... создание этапов ...
  
  // Проверяем, был ли создан этап "Расстановка логгеров"
  const loggerPlacementCreated = stagesToCreate.some(stage => stage.name === 'Расстановка логгеров');
  
  if (loggerPlacementCreated) {
    console.log('Создан этап "Расстановка логгеров", создаем зону "Внешний датчик"');
    // Автоматически создаем зону измерения "Внешний датчик"
    await this.createExternalSensorZone(qualificationObjectId);
  }
  
  return allStagesResult;
}
```

**Автоматическое создание при создании отдельного этапа:**

```typescript
async createWorkStage(qualificationObjectId: string, stageData: any): Promise<QualificationWorkStage> {
  // ... создание этапа ...
  
  const createdStage = this.mapFromDatabase(data);
  
  // Если создан этап "Расстановка логгеров", автоматически создаем зону "Внешний датчик"
  if (stageData.stageName === 'Расстановка логгеров') {
    console.log('Создан этап "Расстановка логгеров", создаем зону "Внешний датчик"');
    await this.createExternalSensorZone(qualificationObjectId);
  }

  return createdStage;
}
```

## 🔧 **Технические детали**

### Структура создаваемой зоны:

```json
{
  "id": "zone-external-{timestamp}",
  "zoneNumber": 0,
  "measurementLevels": [
    {
      "id": "level-external-{timestamp}",
      "level": 1.0,
      "equipmentId": "equipment-external-{timestamp}",
      "equipmentName": "Внешний датчик"
    }
  ]
}
```

### Параметры зоны:

- **ID зоны:** `zone-external-{timestamp}` - уникальный идентификатор
- **Номер зоны:** Всегда 0 (специальный номер для внешнего датчика)
- **Высота датчика:** 1.0 метр от пола (стандартная высота для внешних датчиков)
- **Название оборудования:** "Внешний датчик"

### Проверка дублирования:

- Система проверяет, не существует ли уже зона с датчиком, содержащим слово "внешний"
- Если зона уже существует, создание пропускается
- Предотвращает создание дублирующих зон

### Логика нумерации зон:

- **Зона "Внешний датчик":** Всегда имеет номер 0
- **Пользовательские зоны:** Начинаются с номера 1 и увеличиваются последовательно
- **Независимая нумерация:** Пользовательские зоны нумеруются независимо от зоны 0
- **Пример:** Если есть зоны 0, 1, 2, то следующая пользовательская зона получит номер 3

## 🎯 **Сценарии работы**

### ✅ **Автоматическое создание:**

1. **Создание нового объекта квалификации:**
   - Пользователь создает объект "VW Crafter"
   - Система автоматически создает все 7 этапов квалификационных работ
   - При создании этапа "Расстановка логгеров" автоматически создается зона "Внешний датчик"

2. **Добавление этапа вручную:**
   - Пользователь добавляет этап "Расстановка логгеров" к существующему объекту
   - Система автоматически создает зону "Внешний датчик"

3. **Импорт/восстановление этапов:**
   - При восстановлении этапов из резервной копии
   - При импорте данных из другого проекта
   - Автоматическое создание зоны при обнаружении этапа "Расстановка логгеров"

### 🛡️ **Защита от дублирования:**

- ✅ Зона создается только один раз
- ✅ Проверка существования перед созданием
- ✅ Безопасное выполнение (ошибки не прерывают основной процесс)

## 📋 **Логирование и отладка**

Добавлено подробное логирование:

```typescript
console.log('Создание зоны измерения "Внешний датчик" для объекта:', qualificationObjectId);
console.log('Зона "Внешний датчик" уже существует');
console.log('Зона "Внешний датчик" успешно создана');
console.error('Ошибка создания зоны "Внешний датчик":', error);
```

## 🚀 **Преимущества реализации**

1. **Автоматизация:** Не требует ручного создания зоны измерения
2. **Консистентность:** Одинаковая структура зоны для всех объектов
3. **Безопасность:** Защита от дублирования и ошибок
4. **Гибкость:** Работает для всех типов объектов квалификации
5. **Отладка:** Подробное логирование для диагностики

## 🔄 **Интеграция с существующей системой**

- ✅ **Совместимость:** Полная совместимость с существующим функционалом
- ✅ **Обратная совместимость:** Не влияет на существующие зоны измерения
- ✅ **Модульность:** Логика изолирована в отдельной функции
- ✅ **Расширяемость:** Легко добавить создание других типов зон

## 📝 **Пример использования**

```typescript
// Создание всех этапов для объекта "VW Crafter"
const stages = await qualificationWorkScheduleService.createAllStages(
  'object-id-vw-crafter',
  'project-id'
);

// Автоматически создается:
// 1. Этап "Расстановка логгеров"
// 2. Зона измерения "Внешний датчик" с номером 0 и высотой 1.0м

// При добавлении пользовательских зон:
// - Первая пользовательская зона получит номер 1
// - Вторая пользовательская зона получит номер 2
// - И так далее...
```

## 🎯 **Результат**

Теперь при создании этапа "Расстановка логгеров" в плане графика проведения квалификационных работ для объекта "VW Crafter" (и любых других объектов) автоматически создается зона измерения "Внешний датчик" с номером 0 и высотой 1 метр от пола. Пользовательские зоны измерения начинаются с номера 1 и нумеруются последовательно, что обеспечивает логичную и понятную структуру зон измерения.

