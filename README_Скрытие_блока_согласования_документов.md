# Скрытие блока "Согласование документов" при открытии объекта квалификации

## 🎯 **Задача**

При переходе на страницу редактирования или просмотра объекта квалификации из таблицы "Объекты квалификации" не отображать блок "Согласование документов" для улучшения фокуса пользователя на текущей задаче.

## 🔧 **Выполненные изменения**

### **1. Добавление состояния для отслеживания открытого объекта квалификации**

**В файле `src/components/ContractNegotiation.tsx`:**

**Добавлено новое состояние:**
```typescript
const [isQualificationObjectOpen, setIsQualificationObjectOpen] = useState(false);
```

**Передача функции обновления состояния в QualificationObjectsCRUD:**
```typescript
<QualificationObjectsCRUD 
  contractorId={currentProject.contractorId}
  contractorName={currentProject.contractorName || 'Неизвестный контрагент'}
  projectId={currentProject.id}
  project={currentProject}
  projectQualificationObjects={currentProject.qualificationObjects}
  isCheckboxesBlocked={(() => {
    const contractDoc = documents.find(doc => doc.documentType === 'contract');
    return contractDoc ? approvedDocuments.has(contractDoc.id) : false;
  })()}
  onQualificationObjectStateChange={setIsQualificationObjectOpen}
/>
```

### **2. Условное отображение блока "Согласование документов"**

**Обновлен блок "Согласование документов":**
```typescript
{/* Document Approval - Accordion Block - скрывается при открытом объекте квалификации */}
{!isQualificationObjectOpen && (
  <Accordion
    title="Согласование документов"
    icon={<FileCheck className="w-5 h-5 text-indigo-600" />}
    expanded={isDocumentAccordionExpanded}
    onToggle={setIsDocumentAccordionExpanded}
    className="shadow-sm"
  >
    <div className="p-6">
      <DocumentApproval
        project={currentProject}
        commercialOfferDoc={commercialOfferDoc}
        contractDoc={contractDoc}
        qualificationProtocols={qualificationProtocols}
        approvedDocuments={approvedDocuments}
        documentApprovals={documentApprovals}
        selectedQualificationObjects={currentProject.qualificationObjects?.map(obj => ({
          id: obj.qualificationObjectId,
          type: obj.qualificationObjectType || 'unknown',
          name: obj.qualificationObjectName || 'Без названия'
        })) || []}
        onUpload={handleFileUpload}
        onDelete={handleDeleteDocument}
        onApprove={handleApproveDocument}
        onUnapprove={handleUnapproveDocument}
        onProjectStatusChange={handleProjectStatusChange}
        onProjectUpdate={(updatedProject) => {
          console.log('ContractNegotiation: Обновление проекта через onProjectUpdate:', {
            oldProject: currentProject,
            newProject: updatedProject,
            contractNumber: updatedProject.contractNumber,
            contractDate: updatedProject.contractDate
          });
          setCurrentProject(updatedProject);
        }}
        userRole={user?.role}
      />
    </div>
  </Accordion>
)}
```

### **3. Обновление компонента QualificationObjectsCRUD**

**В файле `src/components/contract/QualificationObjectsCRUD.tsx`:**

**Обновлен интерфейс:**
```typescript
interface QualificationObjectsCRUDProps {
  contractorId: string;
  contractorName: string;
  projectId?: string;
  project?: any;
  projectQualificationObjects?: Array<{
    id: string;
    projectId: string;
    qualificationObjectId: string;
    qualificationObjectName?: string;
    qualificationObjectType?: string;
    createdAt: Date;
  }>;
  qualificationProtocols?: QualificationProtocolWithDocument[];
  isCheckboxesBlocked?: boolean;
  onPageChange?: (page: string, data?: any) => void;
  onQualificationObjectStateChange?: (isOpen: boolean) => void; // Новый проп
}
```

**Добавлен useEffect для отслеживания изменений состояния:**
```typescript
// Отслеживание изменений состояния объекта квалификации
useEffect(() => {
  const isObjectOpen = editingObject !== null || viewingObject !== null;
  if (onQualificationObjectStateChange) {
    onQualificationObjectStateChange(isObjectOpen);
  }
}, [editingObject, viewingObject, onQualificationObjectStateChange]);
```

## 🔄 **Что изменилось**

### **До изменения:**
- ❌ Блок "Согласование документов" отображался всегда
- ❌ Отвлекающие элементы при работе с объектами квалификации
- ❌ Перегруженный интерфейс
- ❌ Сложность фокусировки на текущей задаче

### **После изменения:**
- ✅ Блок "Согласование документов" скрывается при открытии объекта квалификации
- ✅ Чистый интерфейс при работе с объектами
- ✅ Улучшенный фокус на текущей задаче
- ✅ Автоматическое управление видимостью блоков

## 📊 **Логика работы**

### **Состояния отображения:**
```
Состояние объекта квалификации:
├── editingObject === null && viewingObject === null
│   └── isQualificationObjectOpen = false
│       └── Блок "Согласование документов" ОТОБРАЖАЕТСЯ
└── editingObject !== null || viewingObject !== null
    └── isQualificationObjectOpen = true
        └── Блок "Согласование документов" СКРЫТ
```

### **Триггеры изменения состояния:**
```
Открытие объекта квалификации:
├── Клик на иконку 👁️ (просмотр)
│   └── setViewingObject(obj) + setObjectMode('view')
│       └── useEffect → onQualificationObjectStateChange(true)
└── Клик на иконку ▶️ (выполнить)
    └── setEditingObject(obj) + setObjectMode('edit')
        └── useEffect → onQualificationObjectStateChange(true)

Закрытие объекта квалификации:
├── Клик на кнопку "Назад"
│   └── setEditingObject(null) + setViewingObject(null) + setObjectMode(null)
│       └── useEffect → onQualificationObjectStateChange(false)
└── Клик на кнопку "Закрыть"/"Отмена"
    └── setEditingObject(null) + setViewingObject(null) + setObjectMode(null)
        └── useEffect → onQualificationObjectStateChange(false)
```

## 🎯 **Преимущества изменения**

### **1. 🎨 Улучшенный пользовательский опыт:**
- Фокус на текущей задаче без отвлекающих элементов
- Чистый интерфейс при работе с объектами квалификации
- Логичное управление видимостью блоков

### **2. 📱 Оптимизация интерфейса:**
- Уменьшение визуального шума
- Более эффективное использование пространства экрана
- Интуитивное поведение интерфейса

### **3. 🔧 Техническая надежность:**
- Автоматическое управление состоянием
- Реактивное обновление интерфейса
- Консистентное поведение

### **4. 💡 Улучшенная навигация:**
- Понятная логика отображения блоков
- Соответствие ожиданиям пользователя
- Упрощенная работа с интерфейсом

## 🧪 **Тестирование**

### **Для проверки изменений:**

1. **Проверьте отображение блока "Согласование документов" по умолчанию:**
   - Откройте страницу "Согласование договора"
   - Убедитесь, что блок "Согласование документов" отображается
   - Проверьте, что таблица "Объекты квалификации" видна

2. **Проверьте скрытие блока при открытии объекта квалификации:**
   - Нажмите на иконку 👁️ (просмотр) в таблице объектов
   - Убедитесь, что блок "Согласование документов" исчез
   - Проверьте, что форма объекта квалификации отображается

3. **Проверьте скрытие блока при редактировании объекта:**
   - Нажмите на иконку ▶️ (выполнить) в таблице объектов
   - Убедитесь, что блок "Согласование документов" исчез
   - Проверьте, что форма редактирования отображается

4. **Проверьте восстановление блока при закрытии объекта:**
   - Нажмите кнопку "Назад" в форме объекта
   - Убедитесь, что блок "Согласование документов" появился снова
   - Проверьте, что таблица объектов видна

5. **Проверьте восстановление блока при отмене:**
   - Откройте объект для редактирования
   - Нажмите кнопку "Отмена"
   - Убедитесь, что блок "Согласование документов" появился снова

### **Ожидаемое поведение:**

**По умолчанию:**
- ✅ Блок "Согласование документов" отображается
- ✅ Таблица "Объекты квалификации" видна
- ✅ Все элементы интерфейса доступны

**При открытии объекта квалификации:**
- ✅ Блок "Согласование документов" скрыт
- ✅ Форма объекта квалификации отображается
- ✅ Фокус на работе с объектом

**При закрытии объекта квалификации:**
- ✅ Блок "Согласование документов" появляется снова
- ✅ Таблица "Объекты квалификации" видна
- ✅ Возврат к исходному состоянию

## 📋 **Связанные файлы**

- `src/components/ContractNegotiation.tsx` - основная логика с условным отображением блока
- `src/components/contract/QualificationObjectsCRUD.tsx` - отслеживание состояния объекта квалификации

## 🎯 **Заключение**

Скрытие блока "Согласование документов" при открытии объекта квалификации полностью реализовано:

- **Условное отображение:** Блок скрывается автоматически при открытии объекта
- **Автоматическое управление:** Состояние отслеживается через useEffect
- **Улучшенный UX:** Фокус на текущей задаче без отвлекающих элементов
- **Реактивность:** Интерфейс обновляется автоматически при изменении состояния

**Результат: блок "Согласование документов" автоматически скрывается при работе с объектами квалификации, обеспечивая чистый и сфокусированный интерфейс!** 🎉



















