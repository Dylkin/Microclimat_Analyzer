# Добавление поля "Должность" в справочник пользователей

## Задача

Добавить поле для ввода текста "Должность" в справочник пользователей, включая:
- Форму добавления нового пользователя
- Форму редактирования существующего пользователя
- Отображение в таблице пользователей

## Изменения

### 1. Обновлен интерфейс `User` в `src/types/User.ts`

```typescript
export interface User {
  id: string;
  fullName: string;
  email: string;
  password: string;
  role: UserRole;
  position?: string; // Добавлено новое поле
  isDefault?: boolean;
}
```

### 2. Обновлены состояния в `src/components/UserDirectory.tsx`

**Состояние `newUser`:**
```typescript
const [newUser, setNewUser] = useState({
  fullName: '',
  email: '',
  password: '',
  role: 'specialist' as UserRole,
  position: '' // Добавлено новое поле
});
```

**Состояние `editUser`:**
```typescript
const [editUser, setEditUser] = useState({
  fullName: '',
  email: '',
  role: 'specialist' as UserRole,
  position: '' // Добавлено новое поле
});
```

### 3. Обновлена функция `handleEditUser`

```typescript
const handleEditUser = (user: User) => {
  setEditUser({
    fullName: user.fullName,
    email: user.email,
    role: user.role,
    position: user.position || '' // Добавлена инициализация поля position
  });
  setEditingUser(user.id);
};
```

### 4. Добавлено поле "Должность" в форму добавления пользователя

```tsx
<div>
  <label className="block text-sm font-medium text-gray-700 mb-1">
    Должность
  </label>
  <input
    type="text"
    value={newUser.position}
    onChange={(e) => setNewUser(prev => ({ ...prev, position: e.target.value }))}
    className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
    placeholder="Введите должность"
    title="Должность пользователя"
    aria-label="Должность пользователя"
  />
</div>
```

### 5. Добавлена колонка "Должность" в таблицу пользователей

**Заголовок таблицы:**
```tsx
<th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
  Должность
</th>
```

**Ячейка в строке таблицы:**
```tsx
<td className="px-6 py-4 whitespace-nowrap">
  {editingUser === user.id ? (
    <input
      type="text"
      value={editUser.position}
      onChange={(e) => setEditUser(prev => ({ ...prev, position: e.target.value }))}
      className="w-full px-2 py-1 border border-gray-300 rounded focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
      placeholder="Введите должность"
      title="Должность пользователя"
      aria-label="Должность пользователя"
    />
  ) : (
    <div className="text-sm text-gray-500">{user.position || 'Не указана'}</div>
  )}
</td>
```

### 6. Обновлен сброс формы после добавления пользователя

```typescript
setNewUser({
  fullName: '',
  email: '',
  password: '',
  role: 'specialist',
  position: '' // Добавлено сброс поля position
});
```

## Результат

### ✅ Что добавлено:

1. **Поле "Должность"** в интерфейс `User` как опциональное поле
2. **Форма добавления** - поле для ввода должности нового пользователя
3. **Форма редактирования** - возможность изменения должности существующего пользователя
4. **Таблица пользователей** - новая колонка "Должность" с отображением и редактированием
5. **Валидация и сброс** - корректная обработка поля при добавлении и редактировании

### 🎯 Функциональность:

- ✅ **Добавление пользователя** - можно указать должность при создании
- ✅ **Редактирование пользователя** - можно изменить должность существующего пользователя
- ✅ **Отображение в таблице** - должность отображается в отдельной колонке
- ✅ **Inline редактирование** - можно редактировать должность прямо в таблице
- ✅ **Fallback значение** - если должность не указана, отображается "Не указана"
- ✅ **Accessibility** - добавлены `title` и `aria-label` для доступности

### 📊 Структура таблицы:

| Пользователь | Email | Роль | **Должность** | Пароль | Действия |
|--------------|-------|------|---------------|--------|----------|
| Иван Иванов  | ivan@example.com | Специалист | **Инженер** | •••••••• | [Редактировать] |

### 🔧 Технические детали:

1. **Опциональное поле** - `position?: string` не является обязательным
2. **Обратная совместимость** - существующие пользователи без должности отображаются корректно
3. **Валидация** - поле не требует обязательного заполнения
4. **Сброс формы** - поле корректно сбрасывается после добавления пользователя
5. **Редактирование** - поле можно редактировать как в форме, так и inline в таблице

## Тестирование

Для проверки функциональности:

1. **Добавление пользователя:**
   - Откройте справочник пользователей
   - Нажмите "Добавить пользователя"
   - Заполните все поля, включая "Должность"
   - Убедитесь, что пользователь добавлен с указанной должностью

2. **Редактирование пользователя:**
   - Нажмите "Редактировать" у существующего пользователя
   - Измените должность в таблице или через форму
   - Сохраните изменения
   - Убедитесь, что должность обновилась

3. **Отображение в таблице:**
   - Убедитесь, что колонка "Должность" отображается
   - Проверьте, что для пользователей без должности показывается "Не указана"
   - Убедитесь, что можно редактировать должность прямо в таблице

**Поле "Должность" успешно добавлено в справочник пользователей!** 🎉



















