# 🔄 Перезапуск Backend-сервера

## Быстрая диагностика

### 1. Проверьте, запущен ли backend:

```bash
# Проверка через PM2
pm2 list

# Проверка через systemd
sudo systemctl status microclimat-api

# Проверка порта напрямую
netstat -tulpn | grep 3001
# или
ss -tulpn | grep 3001
```

### 2. Проверьте health endpoint:

```bash
curl http://localhost:3001/health
```

Должен вернуть: `{"status":"ok","database":"connected"}`

---

## 🔄 Перезапуск через PM2

### Если backend уже запущен через PM2:

```bash
# Перезапуск
pm2 restart microclimat-api
# или
pm2 restart all

# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs microclimat-api --lines 50
```

### Если backend НЕ запущен через PM2:

```bash
# Перейдите в директорию проекта
cd /path/to/Microclimat_Analyzer

# Запустите через PM2
pm2 start npm --name "microclimat-api" -- run server:prod

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск (если еще не настроено)
pm2 startup
# Выполните команду, которую выведет PM2
```

---

## 🔄 Перезапуск через systemd

```bash
# Перезапуск
sudo systemctl restart microclimat-api

# Проверка статуса
sudo systemctl status microclimat-api

# Просмотр логов
sudo journalctl -u microclimat-api -n 50 -f
```

---

## 🚀 Ручной запуск (для тестирования)

```bash
# Перейдите в директорию проекта
cd /path/to/Microclimat_Analyzer

# Убедитесь, что файл .env настроен
cat .env

# Запустите backend
npm run server:prod
```

**Примечание:** При ручном запуске сервер остановится при закрытии терминала. Для production используйте PM2 или systemd.

---

## 🔍 Диагностика проблем

### Backend не запускается:

1. **Проверьте логи:**
   ```bash
   # PM2
   pm2 logs microclimat-api --lines 100
   
   # Systemd
   sudo journalctl -u microclimat-api -n 100
   ```

2. **Проверьте переменные окружения:**
   ```bash
   cat .env
   # Убедитесь, что все переменные заполнены:
   # - DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
   # - PORT=3001
   ```

3. **Проверьте подключение к базе данных:**
   ```bash
   # Убедитесь, что PostgreSQL запущен
   sudo systemctl status postgresql
   
   # Проверьте подключение
   psql -h localhost -U microclimat_user -d microclimat
   ```

4. **Проверьте, не занят ли порт 3001:**
   ```bash
   netstat -tulpn | grep 3001
   # Если порт занят другим процессом, остановите его или измените PORT в .env
   ```

5. **Установите зависимости:**
   ```bash
   cd /path/to/Microclimat_Analyzer
   npm install
   ```

---

## 📝 Полезные команды PM2

```bash
# Список всех процессов
pm2 list

# Остановка
pm2 stop microclimat-api

# Запуск
pm2 start microclimat-api

# Перезапуск
pm2 restart microclimat-api

# Удаление из PM2
pm2 delete microclimat-api

# Просмотр логов в реальном времени
pm2 logs microclimat-api

# Мониторинг
pm2 monit

# Сохранение конфигурации
pm2 save
```

---

## ✅ После перезапуска

1. Проверьте health endpoint:
   ```bash
   curl http://localhost:3001/health
   ```

2. Проверьте в браузере:
   - Откройте `http://192.168.98.42/api/users` (или ваш IP)
   - Должен вернуться JSON с пользователями

3. Проверьте логи на ошибки:
   ```bash
   pm2 logs microclimat-api --lines 20
   ```

---

## 🆘 Если ничего не помогает

1. **Полная переустановка через PM2:**
   ```bash
   pm2 delete microclimat-api
   cd /path/to/Microclimat_Analyzer
   npm install
   pm2 start npm --name "microclimat-api" -- run server:prod
   pm2 save
   ```

2. **Проверьте версию Node.js:**
   ```bash
   node --version
   # Должна быть 18 или выше
   ```

3. **Проверьте права доступа:**
   ```bash
   ls -la /path/to/Microclimat_Analyzer
   # Убедитесь, что у пользователя есть права на чтение/запись
   ```

