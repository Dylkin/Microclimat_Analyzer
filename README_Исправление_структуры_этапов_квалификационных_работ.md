# Исправление структуры этапов квалификационных работ

## Проблема

В блоке "План-график проведения квалификационных работ" отображалось неверное количество этапов (18 вместо 7) в неправильном порядке. Проблема была вызвана:

1. **Неправильным количеством этапов** - в коде было определено 9 этапов вместо 7
2. **Дублированием этапов** - в базе данных накопились дублирующиеся записи для разных проектов
3. **Отсутствием фильтрации** - система загружала все этапы без удаления дубликатов

## Решение

### 1. Исправлена структура этапов в коде

**В `src/components/QualificationWorkSchedule.tsx`:**
- Удалены этапы "Проверка наличия документации" и "Документы по испытанию"
- Оставлено ровно 7 этапов в правильном порядке:

```typescript
const QUALIFICATION_STAGES = [
  {
    name: 'Расстановка логгеров',
    description: 'Установка и настройка логгеров для мониторинга температуры'
  },
  {
    name: 'Испытание на соответствие критериям в пустом объеме',
    description: 'Проверка соответствия температурных характеристик в пустом состоянии'
  },
  {
    name: 'Испытание на соответствие критериям в загруженном объеме',
    description: 'Проверка соответствия температурных характеристик при полной загрузке'
  },
  {
    name: 'Испытание на восстановление температуры после открытия двери',
    description: 'Проверка времени восстановления температурного режима после открытия'
  },
  {
    name: 'Испытание на отключение электропитания',
    description: 'Проверка поведения системы при отключении электропитания'
  },
  {
    name: 'Испытание на включение электропитания',
    description: 'Проверка запуска системы после включения электропитания'
  },
  {
    name: 'Снятие логгеров',
    description: 'Демонтаж и извлечение логгеров после завершения испытаний'
  }
];
```

**В `src/utils/qualificationWorkScheduleService.ts`:**
- Обновлена функция `createAllStages` для создания только 7 этапов
- Удалены лишние этапы из определения

### 2. Добавлена фильтрация дубликатов

**В функции `loadSchedule`:**
```typescript
// Фильтруем этапы по проекту и удаляем дубликаты
const uniqueStages = new Map();
savedStages.forEach(stage => {
  const key = `${stage.stageName}_${stage.qualificationObjectId}`;
  if (!uniqueStages.has(key)) {
    uniqueStages.set(key, stage);
  }
});
const filteredStages = Array.from(uniqueStages.values());
```

### 3. Создан SQL скрипт для очистки базы данных

**Файл `cleanup_duplicate_qualification_stages.sql`:**
- Удаляет этапы "Проверка наличия документации" и "Документы по испытанию"
- Удаляет дублирующиеся этапы, оставляя только один экземпляр каждого
- Показывает статистику после очистки

## Инструкции по применению

### 1. Выполните SQL скрипт в Supabase Dashboard

1. Откройте ваш Supabase Dashboard
2. Перейдите в раздел "SQL Editor"
3. Откройте файл `cleanup_duplicate_qualification_stages.sql`
4. Скопируйте содержимое файла и вставьте его в SQL Editor
5. Нажмите "Run" для выполнения скрипта

### 2. Перезапустите приложение

После выполнения SQL скрипта, перезапустите ваше React приложение, чтобы изменения в коде вступили в силу.

## Результат

### ✅ Что исправлено:

1. **Правильное количество этапов** - теперь отображается ровно 7 этапов
2. **Правильный порядок** - этапы отображаются в корректной последовательности
3. **Удаление дубликатов** - система автоматически фильтрует дублирующиеся этапы
4. **Консистентность** - структура этапов одинакова в коде и базе данных

### 📊 Структура этапов:

1. **Расстановка логгеров** - Установка и настройка логгеров для мониторинга температуры
2. **Испытание на соответствие критериям в пустом объеме** - Проверка соответствия температурных характеристик в пустом состоянии
3. **Испытание на соответствие критериям в загруженном объеме** - Проверка соответствия температурных характеристик при полной загрузке
4. **Испытание на восстановление температуры после открытия двери** - Проверка времени восстановления температурного режима после открытия
5. **Испытание на отключение электропитания** - Проверка поведения системы при отключении электропитания
6. **Испытание на включение электропитания** - Проверка запуска системы после включения электропитания
7. **Снятие логгеров** - Демонтаж и извлечение логгеров после завершения испытаний

### 🔧 Технические улучшения:

- **Фильтрация дубликатов** - автоматическое удаление повторяющихся этапов
- **Логирование** - подробные логи для диагностики проблем
- **Обратная совместимость** - система работает с существующими данными
- **Производительность** - оптимизированная загрузка этапов

## Тестирование

После применения изменений:

1. **Откройте объект квалификации** в разделе "Управление проектами"
2. **Перейдите в "План-график проведения квалификационных работ"**
3. **Убедитесь, что отображается ровно 7 этапов** в правильном порядке
4. **Проверьте, что нет дублирующихся этапов**
5. **Убедитесь, что функциональность работает корректно** (добавление дат, завершение этапов)

**Структура этапов квалификационных работ успешно исправлена!** 🎉



















