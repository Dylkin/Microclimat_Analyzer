# Добавление поддержки XLS файлов в блок "Снятие логгеров"

## 🎯 **Задача**

Добавить в блок "Снятие логгеров" возможность загрузки и парсинга файлов в формате XLS для сохранения значений температуры и/или влажности и дальнейшей их обработки.

## 🔧 **Выполненные изменения**

### **1. Создан новый парсер для XLS файлов**

**Создан файл `src/utils/xlsParser.ts`:**

**Основной класс `XLSParser`:**
```typescript
export class XLSParser {
  private buffer: ArrayBuffer;
  
  constructor(buffer: ArrayBuffer) {
    this.buffer = buffer;
  }

  async parse(fileName: string): Promise<ParsedFileData> {
    // Парсинг Excel файла с использованием библиотеки xlsx
  }
}
```

**Функциональность парсера:**
- ✅ Чтение Excel файлов (.xls, .xlsx)
- ✅ Автоматическое определение колонок с датой/временем и температурой
- ✅ Поддержка влажности (опционально)
- ✅ Валидация данных
- ✅ Обработка различных форматов дат
- ✅ Поддержка Excel serial dates
- ✅ Обработка ошибок и валидация

### **2. Установлены необходимые зависимости**

**Добавлены пакеты:**
```bash
npm install xlsx
npm install --save-dev @types/xlsx
```

### **3. Обновлен компонент QualificationWorkSchedule**

**В файле `src/components/QualificationWorkSchedule.tsx`:**

**Добавлен импорт XLS парсера:**
```typescript
import { XLSParser } from '../utils/xlsParser';
```

**Обновлена функция загрузки файлов:**
```typescript
// Если это файл .vi2 или .xls/.xlsx, запускаем парсинг
if (['.vi2', '.xls', '.xlsx'].includes(fileExtension)) {
  await handleFileParsing(levelId, file, fileExtension);
}
```

**Переименована и обновлена функция парсинга:**
```typescript
// Было: handleVI2FileParsing
// Стало: handleFileParsing
const handleFileParsing = async (levelId: string, file: File, fileExtension: string) => {
  // Выбираем парсер в зависимости от типа файла
  let parsedData;
  if (fileExtension === '.vi2') {
    console.log('Используем VI2 парсер');
    const vi2ParsingService = new VI2ParsingService();
    parsedData = await vi2ParsingService.parseFile(file);
  } else if (['.xls', '.xlsx'].includes(fileExtension)) {
    console.log('Используем XLS парсер');
    const arrayBuffer = await file.arrayBuffer();
    const xlsParser = new XLSParser(arrayBuffer);
    parsedData = await xlsParser.parse(file.name);
  } else {
    throw new Error(`Неподдерживаемый тип файла: ${fileExtension}`);
  }
}
```

**Обновлена информация о поддерживаемых файлах:**
```typescript
<li>• Файлы .vi2, .xls, .xlsx автоматически обрабатываются и сохраняются в базу данных для анализа</li>
<li>• XLS файлы должны содержать колонки с датой/временем и температурой (влажность опционально)</li>
```

## 📊 **Функциональность XLS парсера**

### **1. Автоматическое определение колонок**

**Поддерживаемые заголовки для даты/времени:**
- `время`, `time`, `timestamp`
- `дата`, `date`
- Комбинации даты и времени

**Поддерживаемые заголовки для температуры:**
- `температура`, `temp`, `t°`, `°c`, `°с`

**Поддерживаемые заголовки для влажности:**
- `влажность`, `humidity`, `rh`, `%`

### **2. Поддержка форматов дат**

**Excel serial dates:**
- Автоматическое преобразование числовых значений Excel в даты
- Корректировка ошибки високосного года Excel

**Строковые форматы:**
- `DD.MM.YYYY HH:mm:ss`
- `DD.MM.YYYY HH:mm`
- `DD.MM.YYYY`
- `YYYY-MM-DD HH:mm:ss`
- `YYYY-MM-DD HH:mm`
- `YYYY-MM-DD`
- `MM/DD/YYYY HH:mm:ss`
- `MM/DD/YYYY HH:mm`
- `MM/DD/YYYY`

### **3. Валидация данных**

**Валидация температуры:**
- Диапазон: -50°C до 100°C
- Проверка на числовые значения

**Валидация влажности:**
- Диапазон: 0% до 100%
- Опциональное поле

**Валидация дат:**
- Проверка корректности даты
- Обработка ошибок парсинга

### **4. Обработка ошибок**

**Типы ошибок:**
- Отсутствие колонок с датой/временем
- Отсутствие колонки с температурой
- Некорректные данные в строках
- Неподдерживаемые форматы файлов

**Логирование:**
- Детальное логирование процесса парсинга
- Информация о найденных колонках
- Статистика обработанных записей

## 🔄 **Что изменилось**

### **До изменения:**
- ❌ Поддерживались только VI2 файлы для автоматического парсинга
- ❌ XLS файлы загружались, но не обрабатывались
- ❌ Нет возможности анализа данных из Excel файлов
- ❌ Ограниченная поддержка форматов данных

### **После изменения:**
- ✅ Поддерживаются VI2, XLS и XLSX файлы для автоматического парсинга
- ✅ XLS файлы автоматически обрабатываются и сохраняются в БД
- ✅ Возможность анализа данных из Excel файлов
- ✅ Гибкая поддержка различных форматов данных
- ✅ Автоматическое определение структуры данных

## 📋 **Требования к XLS файлам**

### **Обязательные колонки:**
1. **Дата/время** - колонка с временными метками
2. **Температура** - колонка с температурными значениями

### **Опциональные колонки:**
1. **Влажность** - колонка с влажностью (если есть)

### **Формат данных:**
- **Заголовки:** Первая строка должна содержать названия колонок
- **Данные:** Начиная со второй строки
- **Разделители:** Автоматическое определение
- **Кодировка:** UTF-8

### **Пример структуры XLS файла:**
```
| Время                | Температура | Влажность |
|---------------------|-------------|-----------|
| 01.01.2024 10:00:00 | 22.5        | 45        |
| 01.01.2024 10:01:00 | 22.6        | 46        |
| 01.01.2024 10:02:00 | 22.4        | 45        |
```

## 🧪 **Тестирование**

### **Для проверки функциональности:**

1. **Подготовьте XLS файл с данными:**
   - Создайте Excel файл с колонками "Время", "Температура", "Влажность"
   - Добавьте несколько строк с тестовыми данными
   - Сохраните в формате .xls или .xlsx

2. **Загрузите файл в систему:**
   - Откройте страницу "Редактировать объект квалификации"
   - Перейдите к этапу "Снятие логгеров"
   - Нажмите "Загрузить" для нужной зоны измерения
   - Выберите подготовленный XLS файл

3. **Проверьте обработку:**
   - ✅ Файл должен загрузиться без ошибок
   - ✅ Должен появиться статус "Обработка..."
   - ✅ После обработки статус должен измениться на "Обработан"
   - ✅ В консоли должны появиться логи парсинга

4. **Проверьте сохранение данных:**
   - ✅ Данные должны сохраниться в базу данных
   - ✅ Файл должен появиться в таблице "Файлы данных логгеров"
   - ✅ Статус должен быть "Обработан"

### **Ожидаемое поведение:**

**При загрузке XLS файла:**
- ✅ Файл принимается системой
- ✅ Запускается XLS парсер
- ✅ Автоматически определяются колонки
- ✅ Данные парсятся и валидируются
- ✅ Результат сохраняется в БД

**При ошибках:**
- ✅ Показывается понятное сообщение об ошибке
- ✅ Логируется детальная информация
- ✅ Система продолжает работать

## 📁 **Измененные файлы**

- `src/utils/xlsParser.ts` - новый парсер для XLS файлов
- `src/components/QualificationWorkSchedule.tsx` - обновлена логика загрузки и парсинга
- `package.json` - добавлены зависимости xlsx и @types/xlsx

## 🎯 **Преимущества изменения**

### **1. 🔧 Расширенная функциональность:**
- Поддержка популярного формата Excel
- Автоматическая обработка данных
- Гибкая структура данных

### **2. 📊 Улучшенная совместимость:**
- Работа с данными из различных источников
- Поддержка экспорта из других систем
- Универсальность формата

### **3. 🎨 Улучшенный пользовательский опыт:**
- Простота загрузки данных
- Автоматическая обработка
- Понятные сообщения об ошибках

### **4. 💡 Техническая надежность:**
- Валидация данных
- Обработка ошибок
- Детальное логирование

## 🔍 **Логирование и отладка**

### **Логи парсинга XLS файлов:**
```
XLSParser: Начинаем анализ XLS файла: data.xlsx
XLSParser: Размер файла: 12345 байт
XLSParser: Найдено листов: ["Sheet1"]
XLSParser: Загружено строк: 100
XLSParser: Заголовки: ["Время", "Температура", "Влажность"]
XLSParser: Индексы колонок: {timestamp: 0, temperature: 1, humidity: 2}
XLSParser: Парсинг завершен: {fileName: "data.xlsx", recordCount: 99, ...}
```

### **Логи в QualificationWorkSchedule:**
```
Начинаем парсинг файла: {fileName: "data.xlsx", fileExtension: ".xlsx", ...}
Используем XLS парсер
Парсинг завершен: {parsingStatus: "completed", recordCount: 99, ...}
```

## 🎯 **Заключение**

Поддержка XLS файлов в блоке "Снятие логгеров" полностью реализована:

- **Новый парсер:** Создан `XLSParser` для обработки Excel файлов
- **Универсальная обработка:** Обновлена логика для поддержки VI2 и XLS файлов
- **Автоматическое определение:** Парсер автоматически находит нужные колонки
- **Валидация данных:** Проверка корректности температуры и влажности
- **Обработка ошибок:** Детальное логирование и понятные сообщения

**Результат: пользователи могут загружать XLS файлы с данными логгеров, которые автоматически обрабатываются и сохраняются для дальнейшего анализа!** 🎉



















