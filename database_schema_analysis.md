# Анализ структуры базы данных Microclimat Analyzer

## 📊 **Обзор таблиц и их взаимосвязей**

### 🏗️ **Основные таблицы**

#### 1. **Пользователи и аутентификация**
- `auth.users` - пользователи Supabase Auth
- `users` - расширенная информация о пользователях
- `audit_logs` - логи аудита действий пользователей

#### 2. **Контрагенты и контакты**
- `contractors` - справочник контрагентов
- `contractor_contacts` - контакты контрагентов

#### 3. **Проекты квалификации**
- `projects` - основная таблица проектов
- `project_qualification_objects` - связь проектов с объектами квалификации
- `project_stage_assignments` - назначения исполнителей на этапы
- `project_equipment_assignments` - назначения оборудования на проекты
- `project_documents` - документы проектов
- `project_activities` - активности проекта

#### 4. **Объекты квалификации**
- `qualification_objects` - объекты квалификации
- `qualification_stages` - этапы квалификации
- `qualification_object_testing_periods` - периоды испытаний объектов

#### 5. **Оборудование и измерения**
- `measurement_equipment` - справочник измерительного оборудования
- `uploaded_files` - загруженные файлы

#### 6. **Уведомления**
- `notifications` - уведомления пользователей

## 🔗 **Схема взаимосвязей**

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   contractors   │    │   projects       │    │   users         │
│                 │    │                  │    │                 │
│ id (PK)         │◄───┤ contractor_id    │    │ id (PK)         │
│ name            │    │ manager_id ──────┼───►│ role            │
│ address         │    │ created_by ──────┼───►│ name            │
│ latitude        │    │ status           │    │ email           │
│ longitude       │    │ contract_number  │    │                 │
│ geocoded_at     │    │ contract_date    │    │                 │
└─────────────────┘    └──────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│contractor_      │    │project_          │    │audit_logs       │
│contacts         │    │qualification_    │    │                 │
│                 │    │objects           │    │ user_id ────────┼───► users
│ contractor_id ──┼───►│                  │    │ user_name       │
│ employee_name   │    │ project_id ──────┼───►│ user_role       │
│ phone           │    │ qualification_   │    │ action          │
│ comment         │    │ object_id ───────┼───►│ entity_type     │
└─────────────────┘    └──────────────────┘    │ entity_id       │
                                               │ timestamp       │
                                               └─────────────────┘
                                                       │
                                                       ▼
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│qualification_   │    │qualification_    │    │project_         │
│objects          │    │stages            │    │documents        │
│                 │    │                  │    │                 │
│ id (PK)         │◄───┤ object_id        │    │ project_id ─────┼───► projects
│ project_id ─────┼───►│ type             │    │ qualification_  │
│ type            │    │ name             │    │ object_id ──────┼───► qualification_objects
│ name            │    │ status           │    │ document_type   │
│ description     │    │ assignee_id ─────┼───►│ file_name       │
│ overall_status  │    │ assignee_name    │    │ url             │
│ technical_      │    │ planned_start_   │    │ uploaded_by     │
│ parameters      │    │ date             │    │ approved_by     │
│ manufacturer    │    │ planned_end_date │    │ approved_at     │
│ test_data_file_ │    │ actual_start_    │    └─────────────────┘
│ url             │    │ date             │
│ test_data_file_ │    │ actual_end_date  │
│ name            │    │ order_number     │
│ measurement_    │    │ notes            │
│ zones           │    └──────────────────┘
│ shelf_count     │
└─────────────────┘
         │
         ▼
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│qualification_   │    │measurement_      │    │uploaded_files   │
│object_testing_  │    │equipment         │    │                 │
│periods          │    │                  │    │ id (PK)         │
│                 │    │ id (PK)          │    │ file_name       │
│ qualification_  │    │ name             │    │ file_size       │
│ object_id ──────┼───►│ model            │    │ file_type       │
│ planned_start_  │    │ serial_number    │    │ object_type     │
│ date            │    │ manufacturer     │    │ uploaded_by     │
│ planned_end_    │    │ status           │    │ uploaded_at     │
│ date            │    │ calibration_date │    │ url             │
│ actual_start_   │    │ notes            │    │ metadata        │
│ date            │    └──────────────────┘    └─────────────────┘
│ actual_end_date │
│ status          │
│ notes           │
│ created_by ─────┼───► users
└─────────────────┘
```

## 📋 **Детальный анализ таблиц**

### **contractors** (Контрагенты)
```sql
- id (uuid, PK) - уникальный идентификатор
- name (text) - наименование контрагента
- address (text) - адрес
- latitude (numeric) - широта для карты
- longitude (numeric) - долгота для карты
- geocoded_at (timestamptz) - время геокодирования
- created_at, updated_at (timestamptz)
```

### **projects** (Проекты)
```sql
- id (uuid, PK) - уникальный идентификатор
- name (text) - наименование проекта
- description (text) - описание
- contractor_id (uuid, FK) - ссылка на контрагента
- contract_number (text) - номер договора
- contract_date (date) - дата договора
- status (project_status) - статус проекта
- created_by (uuid, FK) - создатель проекта
- created_at, updated_at (timestamptz)
```

### **qualification_objects** (Объекты квалификации)
```sql
- id (uuid, PK) - уникальный идентификатор
- project_id (uuid, FK) - ссылка на проект
- type (qualification_object_type) - тип объекта
- name (text) - наименование
- description (text) - описание
- overall_status (qualification_object_status) - общий статус
- overall_progress (integer) - общий прогресс
- current_stage_id (uuid) - текущий этап
- technical_parameters (jsonb) - технические параметры
- manufacturer (text) - производитель
- test_data_file_url (text) - URL файла данных испытаний
- test_data_file_name (text) - имя файла данных испытаний
- measurement_zones (jsonb) - зоны измерения
- shelf_count (integer) - количество полок (для холодильников)
- created_at, updated_at (timestamptz)
```

### **qualification_stages** (Этапы квалификации)
```sql
- id (uuid, PK) - уникальный идентификатор
- object_id (uuid, FK) - ссылка на объект квалификации
- type (qualification_stage_type) - тип этапа
- name (text) - наименование этапа
- description (text) - описание
- status (qualification_stage_status) - статус этапа
- assignee_id (uuid, FK) - назначенный исполнитель
- assignee_name (text) - имя исполнителя
- estimated_duration (integer) - планируемая длительность
- actual_duration (integer) - фактическая длительность
- start_date, end_date (timestamptz) - фактические даты
- planned_start_date, planned_end_date (timestamptz) - плановые даты
- order_number (integer) - порядковый номер
- is_required (boolean) - обязательность этапа
- notes (text) - заметки
- created_at, updated_at (timestamptz)
```

### **project_qualification_objects** (Связь проектов и объектов)
```sql
- id (uuid, PK) - уникальный идентификатор
- project_id (uuid, FK) - ссылка на проект
- qualification_object_id (uuid, FK) - ссылка на объект квалификации
- created_at (timestamptz)
- UNIQUE(project_id, qualification_object_id)
```

### **project_documents** (Документы проектов)
```sql
- id (uuid, PK) - уникальный идентификатор
- project_id (uuid, FK) - ссылка на проект
- qualification_object_id (uuid, FK) - ссылка на объект квалификации (опционально)
- name (text) - наименование документа
- type (document_type) - тип документа
- file_name (text) - имя файла
- file_size (bigint) - размер файла
- uploaded_by (text) - загрузивший
- url (text) - URL файла
- version (integer) - версия
- is_active (boolean) - активность
- approved_by (text) - утвердивший
- approved_at (timestamptz) - дата утверждения
- uploaded_at (timestamptz) - дата загрузки
- UNIQUE(project_id, qualification_object_id, document_type)
```

### **qualification_object_testing_periods** (Периоды испытаний)
```sql
- id (uuid, PK) - уникальный идентификатор
- qualification_object_id (uuid, FK) - ссылка на объект квалификации
- planned_start_date, planned_end_date (date) - плановые даты
- actual_start_date, actual_end_date (date) - фактические даты
- status (testing_period_status) - статус периода
- notes (text) - заметки
- created_by (uuid, FK) - создатель
- created_at, updated_at (timestamptz)
```

### **uploaded_files** (Загруженные файлы)
```sql
- id (uuid, PK) - уникальный идентификатор
- file_name (text) - имя файла
- file_size (bigint) - размер файла
- file_type (text) - тип файла
- object_type (qualification_object_type) - тип объекта (опционально)
- uploaded_by (uuid, FK) - загрузивший
- uploaded_at (timestamptz) - дата загрузки
- url (text) - URL файла
- metadata (jsonb) - метаданные
```

### **measurement_equipment** (Измерительное оборудование)
```sql
- id (uuid, PK) - уникальный идентификатор
- name (text) - наименование
- model (text) - модель
- serial_number (text) - серийный номер
- manufacturer (text) - производитель
- status (text) - статус
- calibration_date (date) - дата калибровки
- notes (text) - заметки
```

### **project_equipment_assignments** (Назначения оборудования)
```sql
- id (uuid, PK) - уникальный идентификатор
- project_id (uuid, FK) - ссылка на проект
- qualification_object_id (uuid, FK) - ссылка на объект квалификации
- equipment_id (uuid, FK) - ссылка на оборудование
- assigned_at (timestamptz) - дата назначения
- completed_at (timestamptz) - дата завершения
- notes (text) - заметки
- created_at (timestamptz)
- UNIQUE(project_id, qualification_object_id, equipment_id)
```

### **audit_logs** (Логи аудита)
```sql
- id (uuid, PK) - уникальный идентификатор
- user_id (uuid, FK) - ссылка на пользователя
- user_name (text) - имя пользователя
- user_role (text) - роль пользователя
- action (text) - действие
- entity_type (text) - тип сущности
- entity_id (text) - ID сущности
- entity_name (text) - имя сущности
- details (jsonb) - детали
- timestamp (timestamptz) - время действия
- ip_address (inet) - IP адрес
- user_agent (text) - User Agent
- created_at (timestamptz)
```

## 🔒 **Политики безопасности (RLS)**

Все таблицы защищены Row Level Security (RLS) с политиками:
- **Чтение**: Доступ для аутентифицированных пользователей
- **Запись**: Доступ для аутентифицированных пользователей
- **Обновление**: Доступ для аутентифицированных пользователей
- **Удаление**: Доступ для аутентифицированных пользователей

## 📊 **Индексы для производительности**

### **Основные индексы:**
- `idx_contractors_name` - поиск по наименованию контрагента
- `idx_projects_contractor_id` - проекты по контрагенту
- `idx_projects_status` - проекты по статусу
- `idx_qualification_objects_project_id` - объекты по проекту
- `idx_qualification_objects_type` - объекты по типу
- `idx_qualification_stages_object_id` - этапы по объекту
- `idx_qualification_stages_assignee_id` - этапы по исполнителю
- `idx_project_documents_project_id` - документы по проекту
- `idx_uploaded_files_object_type` - файлы по типу объекта
- `idx_audit_logs_user_id` - логи по пользователю
- `idx_audit_logs_timestamp` - логи по времени

### **Составные индексы:**
- `idx_qualification_objects_test_data` - объекты с данными испытаний
- `idx_qualification_objects_measurement_zones` - объекты с зонами измерения
- `idx_project_documents_qualification_object_id` - документы по объекту

## 🎯 **Ключевые особенности архитектуры**

### **1. Нормализация данных**
- Контрагенты отделены от проектов
- Объекты квалификации связаны с проектами
- Документы могут быть привязаны к проекту или объекту

### **2. Гибкость связей**
- Проекты могут содержать несколько объектов квалификации
- Объекты могут иметь несколько этапов
- Документы могут быть на уровне проекта или объекта

### **3. Аудит и отслеживание**
- Полное логирование действий пользователей
- Отслеживание изменений статусов
- История загрузки файлов

### **4. Производительность**
- Оптимизированные индексы
- JSONB для гибких данных
- Эффективные запросы

## ⚠️ **Потенциальные проблемы**

### **1. Дублирование данных**
- `assignee_name` дублирует данные из `users`
- `client_name` дублирует данные из `contractors`

### **2. Сложность запросов**
- Множественные JOIN'ы для получения полной информации
- Необходимость обработки NULL значений

### **3. Целостность данных**
- Отсутствие проверок на уровне приложения
- Возможность создания некорректных связей

## 🔧 **Рекомендации по улучшению**

### **1. Нормализация**
- Удалить дублирующие поля
- Использовать только ID для связей

### **2. Индексы**
- Добавить составные индексы для частых запросов
- Оптимизировать запросы с JSONB

### **3. Ограничения**
- Добавить CHECK constraints для валидации
- Улучшить внешние ключи

### **4. Документация**
- Создать диаграммы ER
- Документировать бизнес-логику

---

*Анализ выполнен: Январь 2025*  
*Версия схемы: Актуальная*















