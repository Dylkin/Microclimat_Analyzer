# Тестирование логирования для диагностики полей договора

## 🔍 Проблема
Данные в блоке "Поля договора" загружаются правильно в первый раз, но затем становятся `undefined` при множественных рендерах компонента.

## 🛠️ Добавленное логирование

### 1. В App.tsx
- Логирование в `handlePageChange` - показывает, какие данные передаются при навигации
- Логирование в `renderPage` для `contract_negotiation` - показывает, какой проект передается в компонент

### 2. В ContractNegotiation.tsx
- Логирование при получении проекта - показывает данные проекта при инициализации
- Логирование в `onProjectUpdate` - показывает, как обновляется проект

### 3. В ContractFields.tsx (уже было)
- Логирование при обновлении данных проекта

## 🧪 Тестирование

### Шаг 1: Очистите консоль браузера
1. Откройте консоль браузера (F12)
2. Очистите консоль (Ctrl+L)

### Шаг 2: Перейдите на страницу с полями договора
1. Выберите проект "PharmDistri - WW Transporter, ХК №1"
2. Нажмите "Выполнить" → "Согласование договора"

### Шаг 3: Проверьте логи в консоли

**Ожидаемые логи (в правильном порядке):**

```javascript
// 1. Навигация к странице
App: handlePageChange вызван: {
  page: "contract_negotiation",
  projectData: {
    id: "36b01543-0e89-4954-956a-77a370f78954",
    name: "PharmDistri - WW Transporter, ХК №1",
    contractNumber: "ВР-15-25",        // ← Должно быть не undefined
    contractDate: "2025-10-01"        // ← Должно быть не undefined
  }
}

// 2. Рендеринг страницы
App: Рендеринг contract_negotiation с проектом: {
  selectedProject: {
    id: "36b01543-0e89-4954-956a-77a370f78954",
    name: "PharmDistri - WW Transporter, ХК №1",
    contractNumber: "ВР-15-25",        // ← Должно быть не undefined
    contractDate: "2025-10-01"        // ← Должно быть не undefined
  }
}

// 3. Инициализация ContractNegotiation
ContractNegotiation: Получен проект: {
  id: "36b01543-0e89-4954-956a-77a370f78954",
  name: "PharmDistri - WW Transporter, ХК №1",
  contractNumber: "ВР-15-25",          // ← Должно быть не undefined
  contractDate: "2025-10-01",          // ← Должно быть не undefined
  project: { ... }
}

// 4. Обновление ContractFields
ContractFields: Обновление данных проекта {
  projectId: "36b01543-0e89-4954-956a-77a370f78954",
  contractNumber: "ВР-15-25",          // ← Должно быть не undefined
  contractDate: Date object,           // ← Должно быть не undefined
  project: { ... }
}
```

## 🚨 Возможные проблемы

### Проблема 1: Данные теряются в App.tsx
**Симптом**: В логах `App: handlePageChange` или `App: Рендеринг contract_negotiation` показывают `undefined`
**Причина**: Проект загружается без полей договора из `ProjectDirectory`

### Проблема 2: Данные теряются в ContractNegotiation
**Симптом**: В логах `ContractNegotiation: Получен проект` показывают `undefined`
**Причина**: Проект передается без полей договора из `App.tsx`

### Проблема 3: Данные теряются в ContractFields
**Симптом**: В логах `ContractFields: Обновление данных проекта` показывают `undefined`
**Причина**: Проект передается без полей договора из `ContractNegotiation`

## 🔧 Диагностика

### Если проблема в App.tsx:
1. Проверьте, как загружается проект в `ProjectDirectory`
2. Убедитесь, что `projectService.getAllProjects()` возвращает поля договора

### Если проблема в ContractNegotiation:
1. Проверьте, правильно ли передается проект из `App.tsx`
2. Убедитесь, что `selectedProject` содержит поля договора

### Если проблема в ContractFields:
1. Проверьте, правильно ли передается проект из `ContractNegotiation`
2. Убедитесь, что `currentProject` содержит поля договора

## 📋 Что нужно сообщить

После тестирования сообщите:

1. **Какие логи видите** в консоли (скопируйте все логи)
2. **На каком этапе** данные становятся `undefined`
3. **Есть ли ошибки** в консоли
4. **Отображаются ли данные** в интерфейсе

## 🎯 Ожидаемый результат

После исправления все логи должны показывать правильные данные:
- `contractNumber: "ВР-15-25"` (не `undefined`)
- `contractDate: Date object` (не `undefined`)

И данные должны отображаться в интерфейсе.



















