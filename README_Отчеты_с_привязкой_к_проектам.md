# Система отчетов с привязкой к проектам и объектам квалификации

## 🎯 **Цель**

Реализована система сохранения и управления отчетами анализа данных, которая обеспечивает:

- ✅ Привязку отчетов к конкретному проекту квалификации
- ✅ Привязку отчетов к конкретному объекту квалификации  
- ✅ Доступ к отчетам только при открытии объекта из соответствующего проекта
- ✅ Сохранение всех данных анализа (графики, маркеры, выводы, настройки)
- ✅ Возможность загрузки и продолжения работы с сохраненными отчетами

## 🗄️ **База данных**

### Создание таблицы отчетов

Выполните SQL скрипт `create_analysis_reports_table.sql` в Supabase SQL Editor:

```sql
-- Создание таблицы analysis_reports
CREATE TABLE IF NOT EXISTS analysis_reports (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  qualification_object_id UUID NOT NULL REFERENCES qualification_objects(id) ON DELETE CASCADE,
  report_name TEXT NOT NULL,
  report_type report_type NOT NULL DEFAULT 'analysis',
  report_url TEXT NOT NULL,
  report_filename TEXT NOT NULL,
  report_data JSONB NOT NULL,
  created_by UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### Политики безопасности (RLS)

Настроены политики Row Level Security для обеспечения доступа только к отчетам проектов, к которым у пользователя есть доступ.

## 🔧 **Реализованные компоненты**

### 1. Сервис отчетов (`src/utils/reportService.ts`)

**Основные функции:**
- `saveReport()` - сохранение отчета в БД
- `getReportsByProjectAndObject()` - получение отчетов для конкретного проекта и объекта
- `getReportsByProject()` - получение всех отчетов проекта
- `deleteReport()` - удаление отчета
- `updateReport()` - обновление отчета

**Структура данных отчета:**
```typescript
interface ReportData {
  id?: string;
  projectId: string;
  qualificationObjectId: string;
  reportName: string;
  reportType: 'template' | 'analysis';
  reportUrl: string;
  reportFilename: string;
  reportData: {
    dataType: string;
    analysisResults: any;
    contractFields: any;
    conclusions: string;
    markers: any[];
    limits: any;
  };
  createdBy: string;
  createdAt: Date;
  updatedAt: Date;
}
```

### 2. Обновленный TimeSeriesAnalyzer

**Новые возможности:**
- Автоматическое сохранение отчетов при генерации
- Загрузка сохраненных отчетов при открытии объекта
- Восстановление состояния анализа из сохраненного отчета
- Управление сохраненными отчетами (загрузка, удаление)

**Новые функции:**
- `loadSavedReports()` - загрузка списка сохраненных отчетов
- `handleLoadSavedReport()` - загрузка конкретного отчета
- `handleDeleteSavedReport()` - удаление отчета

## 🎨 **Пользовательский интерфейс**

### Секция "Сохраненные отчеты"

Добавлена новая секция в интерфейс анализа данных:

- **Заголовок:** "Сохраненные отчеты" с иконкой
- **Список отчетов:** Карточки с информацией о каждом отчете
- **Действия:** Кнопки загрузки и удаления для каждого отчета
- **Состояния:** Индикатор загрузки и пустое состояние

### Информация в карточке отчета:
- Название отчета
- Тип отчета (По шаблону / Анализ)
- Дата и время создания
- Кнопки действий (загрузить, удалить)

## 🔒 **Контроль доступа**

### Принципы безопасности:

1. **Привязка к проекту:** Отчеты доступны только в контексте проекта, к которому они привязаны
2. **Привязка к объекту:** Отчеты привязаны к конкретному объекту квалификации
3. **RLS политики:** Пользователи видят только отчеты проектов, к которым у них есть доступ
4. **Владелец отчета:** Пользователи могут удалять только свои отчеты

### Сценарии доступа:

✅ **Доступ разрешен:**
- Открытие объекта квалификации из проекта, к которому привязан отчет
- Пользователь имеет доступ к проекту
- Отчет создан текущим пользователем (для удаления)

❌ **Доступ запрещен:**
- Открытие объекта квалификации не из проекта отчета
- Пользователь не имеет доступа к проекту
- Попытка удалить чужой отчет

## 📋 **Процесс работы**

### Создание отчета:
1. Пользователь открывает объект квалификации из проекта
2. Выполняет анализ данных (настраивает графики, добавляет маркеры, пишет выводы)
3. Генерирует отчет по шаблону
4. Отчет автоматически сохраняется в БД с привязкой к проекту и объекту

### Загрузка отчета:
1. Пользователь открывает объект квалификации из того же проекта
2. В секции "Сохраненные отчеты" видит список доступных отчетов
3. Нажимает кнопку загрузки отчета
4. Восстанавливается состояние анализа (графики, маркеры, выводы, настройки)
5. Пользователь может продолжить работу или скачать отчет

### Удаление отчета:
1. Пользователь нажимает кнопку удаления в карточке отчета
2. Подтверждает удаление
3. Отчет удаляется из БД
4. Список отчетов обновляется

## 🚀 **Преимущества реализации**

1. **Изоляция данных:** Отчеты изолированы по проектам и объектам
2. **Персистентность:** Все настройки анализа сохраняются
3. **Удобство:** Возможность продолжить работу с сохраненного места
4. **Безопасность:** Контроль доступа на уровне БД и приложения
5. **Масштабируемость:** Легко добавлять новые типы отчетов
6. **Аудит:** Отслеживание создания и изменения отчетов

## 🔄 **Интеграция с существующей системой**

- **Совместимость:** Полная совместимость с существующим функционалом
- **Обратная совместимость:** Старые отчеты продолжают работать
- **Постепенное внедрение:** Новая функциональность активируется при наличии projectId и qualificationObjectId
- **Fallback:** При отсутствии БД отчеты работают в локальном режиме

## 📝 **Логирование и отладка**

Добавлено подробное логирование:
- Сохранение отчетов в БД
- Загрузка списка отчетов
- Восстановление состояния из отчета
- Ошибки доступа и валидации

## 🎯 **Результат**

Теперь сформированные пользователем отчеты:
- ✅ Связаны с проектом квалификации и объектом квалификации
- ✅ Доступны для дальнейшей работы
- ✅ НЕ доступны при открытии объекта не из проекта отчета
- ✅ Сохраняют все данные анализа для продолжения работы
- ✅ Защищены системой контроля доступа










