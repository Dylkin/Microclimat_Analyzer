# Тесты проекта Microclimat Analyzer

## 📋 Обзор

Проект использует Jest и React Testing Library для тестирования компонентов и утилит.

## 🚀 Запуск тестов

```bash
# Запустить все тесты
npm test

# Запустить тесты в режиме watch
npm run test:watch

# Запустить тесты с покрытием
npm run test:coverage

# Запустить тесты для CI
npm run test:ci
```

## 📁 Структура тестов

Тесты находятся в следующих директориях:

- `src/components/__tests__/` - тесты компонентов
- `src/utils/__tests__/` - тесты утилит и сервисов
- `src/components/contract/__tests__/` - тесты компонентов договоров

## ✨ Недавно добавленные тесты

### 1. DataAnalysis.test.tsx

Тесты для компонента `DataAnalysis`:

- ✅ Отображение информации об объекте
- ✅ Отображение плана-графика квалификационных работ
- ✅ Форматирование дат этапов
- ✅ Скрытие плана-графика при отсутствии заполненных этапов
- ✅ Отображение реквизитов договора
- ✅ Отображение информации об объекте квалификации

**Покрытие:**
- Загрузка и отображение данных объекта квалификации
- Интеграция с `qualificationWorkScheduleService`
- Фильтрация этапов с заполненными датами

### 2. TimeSeriesAnalyzerVerificationReport.test.tsx

Тесты для функционала формирования приложения свидетельства о поверке:

- ✅ Отображение кнопки "Сформировать приложение свидетельства о поверке"
- ✅ Проверка состояния кнопки (disabled/enabled)
- ✅ Переименование блока "Сохраненные отчеты" → "Приложения к отчету"
- ✅ Переименование кнопки "Сформировать отчет" → "Сформировать приложение о испытаниях"

**Покрытие:**
- UI элементы для формирования отчетов
- Интеграция с сервисами оборудования и верификаций

### 3. equipmentService.test.ts

Тесты для сервиса работы с оборудованием:

- ✅ Проверка доступности сервиса
- ✅ Получение списка оборудования с пагинацией
- ✅ Поиск оборудования по ID
- ✅ Добавление верификаций к оборудованию
- ✅ Обработка поиска с фильтрами

**Покрытие:**
- CRUD операции с оборудованием
- Работа с верификациями метрологической аттестации

### 4. imageRotation.test.ts

Тесты для функций поворота изображений:

- ✅ Поворот изображения на 90° против часовой стрелки
- ✅ Обработка blob URL
- ✅ Обработка обычных HTTP URL
- ✅ Обработка ошибок загрузки изображений
- ✅ Правильная настройка canvas для поворота

**Покрытие:**
- Манипуляции с изображениями через Canvas API
- Обработка различных типов URL

## 📊 Покрытие кода

Текущие требования к покрытию (настроено в `jest.config.js`):

- **Branches:** 70%
- **Functions:** 70%
- **Lines:** 70%
- **Statements:** 70%

## 🧪 Примеры тестов

### Тест компонента

```typescript
import { render, screen, waitFor } from '@testing-library/react';
import MyComponent from '../MyComponent';

test('отображает компонент', async () => {
  render(<MyComponent />);
  
  await waitFor(() => {
    expect(screen.getByText('Ожидаемый текст')).toBeInTheDocument();
  });
});
```

### Тест сервиса

```typescript
import { myService } from '../myService';

jest.mock('../apiClient');

test('выполняет операцию', async () => {
  const result = await myService.doSomething();
  
  expect(result).toBeDefined();
  expect(mockApiClient.get).toHaveBeenCalled();
});
```

## 🔧 Настройка моков

### Mock для API клиента

```typescript
jest.mock('../utils/apiClient', () => ({
  apiClient: {
    get: jest.fn(),
    post: jest.fn(),
    put: jest.fn(),
    delete: jest.fn()
  }
}));
```

### Mock для React контекста

```typescript
jest.mock('../contexts/AuthContext', () => ({
  useAuth: () => ({
    user: { id: 'test-user', role: 'admin' },
    hasAccess: () => true
  })
}));
```

## 📝 Рекомендации по написанию тестов

1. **Именование:** Используйте описательные имена тестов на русском языке
2. **Изоляция:** Каждый тест должен быть независимым
3. **Моки:** Мокайте внешние зависимости (API, сервисы)
4. **Покрытие:** Стремитесь покрыть основные сценарии использования
5. **Читаемость:** Тесты должны быть понятными и самодокументируемыми

## 🐛 Устранение неполадок

### Проблема: Тесты не находят модули

Убедитесь, что все зависимости установлены:
```bash
npm install
```

### Проблема: Ошибки с моками

Проверьте, что моки правильно настроены и соответствуют реальной структуре модулей.

### Проблема: Ошибки с Canvas/Image

В `setupTests.ts` уже настроены моки для Canvas API. Если нужны дополнительные моки, добавьте их туда.

## 📚 Дополнительные ресурсы

- [Jest Documentation](https://jestjs.io/docs/getting-started)
- [React Testing Library](https://testing-library.com/react)
- [Testing Best Practices](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library)

